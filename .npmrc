# Hoist all dependencies into the root node_modules so the shared dev tooling
# declared in the root package.json (typescript, @types/node, esbuild, ...) is
# resolvable from every workspace package without duplicating it per package.
node-linker=hoisted
