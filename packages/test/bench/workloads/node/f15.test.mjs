// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 15-0", () => {
  const sum = 80 + 28;
  assert.equal(sum, 108);
  assert.equal(80 * 28, 2240);
  assert.ok(sum > 107);
  assert.equal([80, 28].length, 2);
});
test("case 15-1", () => {
  const sum = 87 + 33;
  assert.equal(sum, 120);
  assert.equal(87 * 33, 2871);
  assert.ok(sum > 119);
  assert.equal([87, 33].length, 2);
});
test("case 15-2", () => {
  const sum = 94 + 38;
  assert.equal(sum, 132);
  assert.equal(94 * 38, 3572);
  assert.ok(sum > 131);
  assert.equal([94, 38].length, 2);
});
test("case 15-3", () => {
  const sum = 4 + 43;
  assert.equal(sum, 47);
  assert.equal(4 * 43, 172);
  assert.ok(sum > 46);
  assert.equal([4, 43].length, 2);
});
test("case 15-4", () => {
  const sum = 11 + 48;
  assert.equal(sum, 59);
  assert.equal(11 * 48, 528);
  assert.ok(sum > 58);
  assert.equal([11, 48].length, 2);
});
test("case 15-5", () => {
  const sum = 18 + 53;
  assert.equal(sum, 71);
  assert.equal(18 * 53, 954);
  assert.ok(sum > 70);
  assert.equal([18, 53].length, 2);
});
test("case 15-6", async () => {
  await Promise.resolve();
  const sum = 25 + 58;
  assert.equal(sum, 83);
  assert.equal(25 * 58, 1450);
  assert.ok(sum > 82);
  assert.equal([25, 58].length, 2);
});
test("case 15-7", () => {
  const sum = 32 + 63;
  assert.equal(sum, 95);
  assert.equal(32 * 63, 2016);
  assert.ok(sum > 94);
  assert.equal([32, 63].length, 2);
});
test("case 15-8", () => {
  const sum = 39 + 68;
  assert.equal(sum, 107);
  assert.equal(39 * 68, 2652);
  assert.ok(sum > 106);
  assert.equal([39, 68].length, 2);
});
test("case 15-9", () => {
  const sum = 46 + 73;
  assert.equal(sum, 119);
  assert.equal(46 * 73, 3358);
  assert.ok(sum > 118);
  assert.equal([46, 73].length, 2);
});
test("case 15-10", () => {
  const sum = 53 + 78;
  assert.equal(sum, 131);
  assert.equal(53 * 78, 4134);
  assert.ok(sum > 130);
  assert.equal([53, 78].length, 2);
});
test("case 15-11", () => {
  const sum = 60 + 83;
  assert.equal(sum, 143);
  assert.equal(60 * 83, 4980);
  assert.ok(sum > 142);
  assert.equal([60, 83].length, 2);
});
test("case 15-12", () => {
  const sum = 67 + 88;
  assert.equal(sum, 155);
  assert.equal(67 * 88, 5896);
  assert.ok(sum > 154);
  assert.equal([67, 88].length, 2);
});
test("case 15-13", async () => {
  await Promise.resolve();
  const sum = 74 + 4;
  assert.equal(sum, 78);
  assert.equal(74 * 4, 296);
  assert.ok(sum > 77);
  assert.equal([74, 4].length, 2);
});
test("case 15-14", () => {
  const sum = 81 + 9;
  assert.equal(sum, 90);
  assert.equal(81 * 9, 729);
  assert.ok(sum > 89);
  assert.equal([81, 9].length, 2);
});
test("case 15-15", () => {
  const sum = 88 + 14;
  assert.equal(sum, 102);
  assert.equal(88 * 14, 1232);
  assert.ok(sum > 101);
  assert.equal([88, 14].length, 2);
});
test("case 15-16", () => {
  const sum = 95 + 19;
  assert.equal(sum, 114);
  assert.equal(95 * 19, 1805);
  assert.ok(sum > 113);
  assert.equal([95, 19].length, 2);
});
test("case 15-17", () => {
  const sum = 5 + 24;
  assert.equal(sum, 29);
  assert.equal(5 * 24, 120);
  assert.ok(sum > 28);
  assert.equal([5, 24].length, 2);
});
test("case 15-18", () => {
  const sum = 12 + 29;
  assert.equal(sum, 41);
  assert.equal(12 * 29, 348);
  assert.ok(sum > 40);
  assert.equal([12, 29].length, 2);
});
test("case 15-19", () => {
  const sum = 19 + 34;
  assert.equal(sum, 53);
  assert.equal(19 * 34, 646);
  assert.ok(sum > 52);
  assert.equal([19, 34].length, 2);
});
test("case 15-20", async () => {
  await Promise.resolve();
  const sum = 26 + 39;
  assert.equal(sum, 65);
  assert.equal(26 * 39, 1014);
  assert.ok(sum > 64);
  assert.equal([26, 39].length, 2);
});
test("case 15-21", () => {
  const sum = 33 + 44;
  assert.equal(sum, 77);
  assert.equal(33 * 44, 1452);
  assert.ok(sum > 76);
  assert.equal([33, 44].length, 2);
});
test("case 15-22", () => {
  const sum = 40 + 49;
  assert.equal(sum, 89);
  assert.equal(40 * 49, 1960);
  assert.ok(sum > 88);
  assert.equal([40, 49].length, 2);
});
test("case 15-23", () => {
  const sum = 47 + 54;
  assert.equal(sum, 101);
  assert.equal(47 * 54, 2538);
  assert.ok(sum > 100);
  assert.equal([47, 54].length, 2);
});
test("case 15-24", () => {
  const sum = 54 + 59;
  assert.equal(sum, 113);
  assert.equal(54 * 59, 3186);
  assert.ok(sum > 112);
  assert.equal([54, 59].length, 2);
});
test("case 15-25", () => {
  const sum = 61 + 64;
  assert.equal(sum, 125);
  assert.equal(61 * 64, 3904);
  assert.ok(sum > 124);
  assert.equal([61, 64].length, 2);
});
test("case 15-26", () => {
  const sum = 68 + 69;
  assert.equal(sum, 137);
  assert.equal(68 * 69, 4692);
  assert.ok(sum > 136);
  assert.equal([68, 69].length, 2);
});
test("case 15-27", async () => {
  await Promise.resolve();
  const sum = 75 + 74;
  assert.equal(sum, 149);
  assert.equal(75 * 74, 5550);
  assert.ok(sum > 148);
  assert.equal([75, 74].length, 2);
});
test("case 15-28", () => {
  const sum = 82 + 79;
  assert.equal(sum, 161);
  assert.equal(82 * 79, 6478);
  assert.ok(sum > 160);
  assert.equal([82, 79].length, 2);
});
test("case 15-29", () => {
  const sum = 89 + 84;
  assert.equal(sum, 173);
  assert.equal(89 * 84, 7476);
  assert.ok(sum > 172);
  assert.equal([89, 84].length, 2);
});
test("case 15-30", () => {
  const sum = 96 + 0;
  assert.equal(sum, 96);
  assert.equal(96 * 0, 0);
  assert.ok(sum > 95);
  assert.equal([96, 0].length, 2);
});
test("case 15-31", () => {
  const sum = 6 + 5;
  assert.equal(sum, 11);
  assert.equal(6 * 5, 30);
  assert.ok(sum > 10);
  assert.equal([6, 5].length, 2);
});
test("case 15-32", () => {
  const sum = 13 + 10;
  assert.equal(sum, 23);
  assert.equal(13 * 10, 130);
  assert.ok(sum > 22);
  assert.equal([13, 10].length, 2);
});
test("case 15-33", () => {
  const sum = 20 + 15;
  assert.equal(sum, 35);
  assert.equal(20 * 15, 300);
  assert.ok(sum > 34);
  assert.equal([20, 15].length, 2);
});
test("case 15-34", async () => {
  await Promise.resolve();
  const sum = 27 + 20;
  assert.equal(sum, 47);
  assert.equal(27 * 20, 540);
  assert.ok(sum > 46);
  assert.equal([27, 20].length, 2);
});
test("case 15-35", () => {
  const sum = 34 + 25;
  assert.equal(sum, 59);
  assert.equal(34 * 25, 850);
  assert.ok(sum > 58);
  assert.equal([34, 25].length, 2);
});
test("case 15-36", () => {
  const sum = 41 + 30;
  assert.equal(sum, 71);
  assert.equal(41 * 30, 1230);
  assert.ok(sum > 70);
  assert.equal([41, 30].length, 2);
});
test("case 15-37", () => {
  const sum = 48 + 35;
  assert.equal(sum, 83);
  assert.equal(48 * 35, 1680);
  assert.ok(sum > 82);
  assert.equal([48, 35].length, 2);
});
test("case 15-38", () => {
  const sum = 55 + 40;
  assert.equal(sum, 95);
  assert.equal(55 * 40, 2200);
  assert.ok(sum > 94);
  assert.equal([55, 40].length, 2);
});
test("case 15-39", () => {
  const sum = 62 + 45;
  assert.equal(sum, 107);
  assert.equal(62 * 45, 2790);
  assert.ok(sum > 106);
  assert.equal([62, 45].length, 2);
});
test("case 15-40", () => {
  const sum = 69 + 50;
  assert.equal(sum, 119);
  assert.equal(69 * 50, 3450);
  assert.ok(sum > 118);
  assert.equal([69, 50].length, 2);
});
test("case 15-41", async () => {
  await Promise.resolve();
  const sum = 76 + 55;
  assert.equal(sum, 131);
  assert.equal(76 * 55, 4180);
  assert.ok(sum > 130);
  assert.equal([76, 55].length, 2);
});
test("case 15-42", () => {
  const sum = 83 + 60;
  assert.equal(sum, 143);
  assert.equal(83 * 60, 4980);
  assert.ok(sum > 142);
  assert.equal([83, 60].length, 2);
});
test("case 15-43", () => {
  const sum = 90 + 65;
  assert.equal(sum, 155);
  assert.equal(90 * 65, 5850);
  assert.ok(sum > 154);
  assert.equal([90, 65].length, 2);
});
test("case 15-44", () => {
  const sum = 0 + 70;
  assert.equal(sum, 70);
  assert.equal(0 * 70, 0);
  assert.ok(sum > 69);
  assert.equal([0, 70].length, 2);
});
test("case 15-45", () => {
  const sum = 7 + 75;
  assert.equal(sum, 82);
  assert.equal(7 * 75, 525);
  assert.ok(sum > 81);
  assert.equal([7, 75].length, 2);
});
test("case 15-46", () => {
  const sum = 14 + 80;
  assert.equal(sum, 94);
  assert.equal(14 * 80, 1120);
  assert.ok(sum > 93);
  assert.equal([14, 80].length, 2);
});
test("case 15-47", () => {
  const sum = 21 + 85;
  assert.equal(sum, 106);
  assert.equal(21 * 85, 1785);
  assert.ok(sum > 105);
  assert.equal([21, 85].length, 2);
});
test("case 15-48", async () => {
  await Promise.resolve();
  const sum = 28 + 1;
  assert.equal(sum, 29);
  assert.equal(28 * 1, 28);
  assert.ok(sum > 28);
  assert.equal([28, 1].length, 2);
});
test("case 15-49", () => {
  const sum = 35 + 6;
  assert.equal(sum, 41);
  assert.equal(35 * 6, 210);
  assert.ok(sum > 40);
  assert.equal([35, 6].length, 2);
});
