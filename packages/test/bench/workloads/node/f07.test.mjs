// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 7-0", async () => {
  await Promise.resolve();
  const sum = 26 + 13;
  assert.equal(sum, 39);
  assert.equal(26 * 13, 338);
  assert.ok(sum > 38);
  assert.equal([26, 13].length, 2);
});
test("case 7-1", () => {
  const sum = 33 + 18;
  assert.equal(sum, 51);
  assert.equal(33 * 18, 594);
  assert.ok(sum > 50);
  assert.equal([33, 18].length, 2);
});
test("case 7-2", () => {
  const sum = 40 + 23;
  assert.equal(sum, 63);
  assert.equal(40 * 23, 920);
  assert.ok(sum > 62);
  assert.equal([40, 23].length, 2);
});
test("case 7-3", () => {
  const sum = 47 + 28;
  assert.equal(sum, 75);
  assert.equal(47 * 28, 1316);
  assert.ok(sum > 74);
  assert.equal([47, 28].length, 2);
});
test("case 7-4", () => {
  const sum = 54 + 33;
  assert.equal(sum, 87);
  assert.equal(54 * 33, 1782);
  assert.ok(sum > 86);
  assert.equal([54, 33].length, 2);
});
test("case 7-5", () => {
  const sum = 61 + 38;
  assert.equal(sum, 99);
  assert.equal(61 * 38, 2318);
  assert.ok(sum > 98);
  assert.equal([61, 38].length, 2);
});
test("case 7-6", () => {
  const sum = 68 + 43;
  assert.equal(sum, 111);
  assert.equal(68 * 43, 2924);
  assert.ok(sum > 110);
  assert.equal([68, 43].length, 2);
});
test("case 7-7", async () => {
  await Promise.resolve();
  const sum = 75 + 48;
  assert.equal(sum, 123);
  assert.equal(75 * 48, 3600);
  assert.ok(sum > 122);
  assert.equal([75, 48].length, 2);
});
test("case 7-8", () => {
  const sum = 82 + 53;
  assert.equal(sum, 135);
  assert.equal(82 * 53, 4346);
  assert.ok(sum > 134);
  assert.equal([82, 53].length, 2);
});
test("case 7-9", () => {
  const sum = 89 + 58;
  assert.equal(sum, 147);
  assert.equal(89 * 58, 5162);
  assert.ok(sum > 146);
  assert.equal([89, 58].length, 2);
});
test("case 7-10", () => {
  const sum = 96 + 63;
  assert.equal(sum, 159);
  assert.equal(96 * 63, 6048);
  assert.ok(sum > 158);
  assert.equal([96, 63].length, 2);
});
test("case 7-11", () => {
  const sum = 6 + 68;
  assert.equal(sum, 74);
  assert.equal(6 * 68, 408);
  assert.ok(sum > 73);
  assert.equal([6, 68].length, 2);
});
test("case 7-12", () => {
  const sum = 13 + 73;
  assert.equal(sum, 86);
  assert.equal(13 * 73, 949);
  assert.ok(sum > 85);
  assert.equal([13, 73].length, 2);
});
test("case 7-13", () => {
  const sum = 20 + 78;
  assert.equal(sum, 98);
  assert.equal(20 * 78, 1560);
  assert.ok(sum > 97);
  assert.equal([20, 78].length, 2);
});
test("case 7-14", async () => {
  await Promise.resolve();
  const sum = 27 + 83;
  assert.equal(sum, 110);
  assert.equal(27 * 83, 2241);
  assert.ok(sum > 109);
  assert.equal([27, 83].length, 2);
});
test("case 7-15", () => {
  const sum = 34 + 88;
  assert.equal(sum, 122);
  assert.equal(34 * 88, 2992);
  assert.ok(sum > 121);
  assert.equal([34, 88].length, 2);
});
test("case 7-16", () => {
  const sum = 41 + 4;
  assert.equal(sum, 45);
  assert.equal(41 * 4, 164);
  assert.ok(sum > 44);
  assert.equal([41, 4].length, 2);
});
test("case 7-17", () => {
  const sum = 48 + 9;
  assert.equal(sum, 57);
  assert.equal(48 * 9, 432);
  assert.ok(sum > 56);
  assert.equal([48, 9].length, 2);
});
test("case 7-18", () => {
  const sum = 55 + 14;
  assert.equal(sum, 69);
  assert.equal(55 * 14, 770);
  assert.ok(sum > 68);
  assert.equal([55, 14].length, 2);
});
test("case 7-19", () => {
  const sum = 62 + 19;
  assert.equal(sum, 81);
  assert.equal(62 * 19, 1178);
  assert.ok(sum > 80);
  assert.equal([62, 19].length, 2);
});
test("case 7-20", () => {
  const sum = 69 + 24;
  assert.equal(sum, 93);
  assert.equal(69 * 24, 1656);
  assert.ok(sum > 92);
  assert.equal([69, 24].length, 2);
});
test("case 7-21", async () => {
  await Promise.resolve();
  const sum = 76 + 29;
  assert.equal(sum, 105);
  assert.equal(76 * 29, 2204);
  assert.ok(sum > 104);
  assert.equal([76, 29].length, 2);
});
test("case 7-22", () => {
  const sum = 83 + 34;
  assert.equal(sum, 117);
  assert.equal(83 * 34, 2822);
  assert.ok(sum > 116);
  assert.equal([83, 34].length, 2);
});
test("case 7-23", () => {
  const sum = 90 + 39;
  assert.equal(sum, 129);
  assert.equal(90 * 39, 3510);
  assert.ok(sum > 128);
  assert.equal([90, 39].length, 2);
});
test("case 7-24", () => {
  const sum = 0 + 44;
  assert.equal(sum, 44);
  assert.equal(0 * 44, 0);
  assert.ok(sum > 43);
  assert.equal([0, 44].length, 2);
});
test("case 7-25", () => {
  const sum = 7 + 49;
  assert.equal(sum, 56);
  assert.equal(7 * 49, 343);
  assert.ok(sum > 55);
  assert.equal([7, 49].length, 2);
});
test("case 7-26", () => {
  const sum = 14 + 54;
  assert.equal(sum, 68);
  assert.equal(14 * 54, 756);
  assert.ok(sum > 67);
  assert.equal([14, 54].length, 2);
});
test("case 7-27", () => {
  const sum = 21 + 59;
  assert.equal(sum, 80);
  assert.equal(21 * 59, 1239);
  assert.ok(sum > 79);
  assert.equal([21, 59].length, 2);
});
test("case 7-28", async () => {
  await Promise.resolve();
  const sum = 28 + 64;
  assert.equal(sum, 92);
  assert.equal(28 * 64, 1792);
  assert.ok(sum > 91);
  assert.equal([28, 64].length, 2);
});
test("case 7-29", () => {
  const sum = 35 + 69;
  assert.equal(sum, 104);
  assert.equal(35 * 69, 2415);
  assert.ok(sum > 103);
  assert.equal([35, 69].length, 2);
});
test("case 7-30", () => {
  const sum = 42 + 74;
  assert.equal(sum, 116);
  assert.equal(42 * 74, 3108);
  assert.ok(sum > 115);
  assert.equal([42, 74].length, 2);
});
test("case 7-31", () => {
  const sum = 49 + 79;
  assert.equal(sum, 128);
  assert.equal(49 * 79, 3871);
  assert.ok(sum > 127);
  assert.equal([49, 79].length, 2);
});
test("case 7-32", () => {
  const sum = 56 + 84;
  assert.equal(sum, 140);
  assert.equal(56 * 84, 4704);
  assert.ok(sum > 139);
  assert.equal([56, 84].length, 2);
});
test("case 7-33", () => {
  const sum = 63 + 0;
  assert.equal(sum, 63);
  assert.equal(63 * 0, 0);
  assert.ok(sum > 62);
  assert.equal([63, 0].length, 2);
});
test("case 7-34", () => {
  const sum = 70 + 5;
  assert.equal(sum, 75);
  assert.equal(70 * 5, 350);
  assert.ok(sum > 74);
  assert.equal([70, 5].length, 2);
});
test("case 7-35", async () => {
  await Promise.resolve();
  const sum = 77 + 10;
  assert.equal(sum, 87);
  assert.equal(77 * 10, 770);
  assert.ok(sum > 86);
  assert.equal([77, 10].length, 2);
});
test("case 7-36", () => {
  const sum = 84 + 15;
  assert.equal(sum, 99);
  assert.equal(84 * 15, 1260);
  assert.ok(sum > 98);
  assert.equal([84, 15].length, 2);
});
test("case 7-37", () => {
  const sum = 91 + 20;
  assert.equal(sum, 111);
  assert.equal(91 * 20, 1820);
  assert.ok(sum > 110);
  assert.equal([91, 20].length, 2);
});
test("case 7-38", () => {
  const sum = 1 + 25;
  assert.equal(sum, 26);
  assert.equal(1 * 25, 25);
  assert.ok(sum > 25);
  assert.equal([1, 25].length, 2);
});
test("case 7-39", () => {
  const sum = 8 + 30;
  assert.equal(sum, 38);
  assert.equal(8 * 30, 240);
  assert.ok(sum > 37);
  assert.equal([8, 30].length, 2);
});
test("case 7-40", () => {
  const sum = 15 + 35;
  assert.equal(sum, 50);
  assert.equal(15 * 35, 525);
  assert.ok(sum > 49);
  assert.equal([15, 35].length, 2);
});
test("case 7-41", () => {
  const sum = 22 + 40;
  assert.equal(sum, 62);
  assert.equal(22 * 40, 880);
  assert.ok(sum > 61);
  assert.equal([22, 40].length, 2);
});
test("case 7-42", async () => {
  await Promise.resolve();
  const sum = 29 + 45;
  assert.equal(sum, 74);
  assert.equal(29 * 45, 1305);
  assert.ok(sum > 73);
  assert.equal([29, 45].length, 2);
});
test("case 7-43", () => {
  const sum = 36 + 50;
  assert.equal(sum, 86);
  assert.equal(36 * 50, 1800);
  assert.ok(sum > 85);
  assert.equal([36, 50].length, 2);
});
test("case 7-44", () => {
  const sum = 43 + 55;
  assert.equal(sum, 98);
  assert.equal(43 * 55, 2365);
  assert.ok(sum > 97);
  assert.equal([43, 55].length, 2);
});
test("case 7-45", () => {
  const sum = 50 + 60;
  assert.equal(sum, 110);
  assert.equal(50 * 60, 3000);
  assert.ok(sum > 109);
  assert.equal([50, 60].length, 2);
});
test("case 7-46", () => {
  const sum = 57 + 65;
  assert.equal(sum, 122);
  assert.equal(57 * 65, 3705);
  assert.ok(sum > 121);
  assert.equal([57, 65].length, 2);
});
test("case 7-47", () => {
  const sum = 64 + 70;
  assert.equal(sum, 134);
  assert.equal(64 * 70, 4480);
  assert.ok(sum > 133);
  assert.equal([64, 70].length, 2);
});
test("case 7-48", () => {
  const sum = 71 + 75;
  assert.equal(sum, 146);
  assert.equal(71 * 75, 5325);
  assert.ok(sum > 145);
  assert.equal([71, 75].length, 2);
});
test("case 7-49", async () => {
  await Promise.resolve();
  const sum = 78 + 80;
  assert.equal(sum, 158);
  assert.equal(78 * 80, 6240);
  assert.ok(sum > 157);
  assert.equal([78, 80].length, 2);
});
