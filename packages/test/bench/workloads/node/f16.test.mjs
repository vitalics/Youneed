// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 16-0", () => {
  const sum = 14 + 41;
  assert.equal(sum, 55);
  assert.equal(14 * 41, 574);
  assert.ok(sum > 54);
  assert.equal([14, 41].length, 2);
});
test("case 16-1", () => {
  const sum = 21 + 46;
  assert.equal(sum, 67);
  assert.equal(21 * 46, 966);
  assert.ok(sum > 66);
  assert.equal([21, 46].length, 2);
});
test("case 16-2", () => {
  const sum = 28 + 51;
  assert.equal(sum, 79);
  assert.equal(28 * 51, 1428);
  assert.ok(sum > 78);
  assert.equal([28, 51].length, 2);
});
test("case 16-3", () => {
  const sum = 35 + 56;
  assert.equal(sum, 91);
  assert.equal(35 * 56, 1960);
  assert.ok(sum > 90);
  assert.equal([35, 56].length, 2);
});
test("case 16-4", () => {
  const sum = 42 + 61;
  assert.equal(sum, 103);
  assert.equal(42 * 61, 2562);
  assert.ok(sum > 102);
  assert.equal([42, 61].length, 2);
});
test("case 16-5", async () => {
  await Promise.resolve();
  const sum = 49 + 66;
  assert.equal(sum, 115);
  assert.equal(49 * 66, 3234);
  assert.ok(sum > 114);
  assert.equal([49, 66].length, 2);
});
test("case 16-6", () => {
  const sum = 56 + 71;
  assert.equal(sum, 127);
  assert.equal(56 * 71, 3976);
  assert.ok(sum > 126);
  assert.equal([56, 71].length, 2);
});
test("case 16-7", () => {
  const sum = 63 + 76;
  assert.equal(sum, 139);
  assert.equal(63 * 76, 4788);
  assert.ok(sum > 138);
  assert.equal([63, 76].length, 2);
});
test("case 16-8", () => {
  const sum = 70 + 81;
  assert.equal(sum, 151);
  assert.equal(70 * 81, 5670);
  assert.ok(sum > 150);
  assert.equal([70, 81].length, 2);
});
test("case 16-9", () => {
  const sum = 77 + 86;
  assert.equal(sum, 163);
  assert.equal(77 * 86, 6622);
  assert.ok(sum > 162);
  assert.equal([77, 86].length, 2);
});
test("case 16-10", () => {
  const sum = 84 + 2;
  assert.equal(sum, 86);
  assert.equal(84 * 2, 168);
  assert.ok(sum > 85);
  assert.equal([84, 2].length, 2);
});
test("case 16-11", () => {
  const sum = 91 + 7;
  assert.equal(sum, 98);
  assert.equal(91 * 7, 637);
  assert.ok(sum > 97);
  assert.equal([91, 7].length, 2);
});
test("case 16-12", async () => {
  await Promise.resolve();
  const sum = 1 + 12;
  assert.equal(sum, 13);
  assert.equal(1 * 12, 12);
  assert.ok(sum > 12);
  assert.equal([1, 12].length, 2);
});
test("case 16-13", () => {
  const sum = 8 + 17;
  assert.equal(sum, 25);
  assert.equal(8 * 17, 136);
  assert.ok(sum > 24);
  assert.equal([8, 17].length, 2);
});
test("case 16-14", () => {
  const sum = 15 + 22;
  assert.equal(sum, 37);
  assert.equal(15 * 22, 330);
  assert.ok(sum > 36);
  assert.equal([15, 22].length, 2);
});
test("case 16-15", () => {
  const sum = 22 + 27;
  assert.equal(sum, 49);
  assert.equal(22 * 27, 594);
  assert.ok(sum > 48);
  assert.equal([22, 27].length, 2);
});
test("case 16-16", () => {
  const sum = 29 + 32;
  assert.equal(sum, 61);
  assert.equal(29 * 32, 928);
  assert.ok(sum > 60);
  assert.equal([29, 32].length, 2);
});
test("case 16-17", () => {
  const sum = 36 + 37;
  assert.equal(sum, 73);
  assert.equal(36 * 37, 1332);
  assert.ok(sum > 72);
  assert.equal([36, 37].length, 2);
});
test("case 16-18", () => {
  const sum = 43 + 42;
  assert.equal(sum, 85);
  assert.equal(43 * 42, 1806);
  assert.ok(sum > 84);
  assert.equal([43, 42].length, 2);
});
test("case 16-19", async () => {
  await Promise.resolve();
  const sum = 50 + 47;
  assert.equal(sum, 97);
  assert.equal(50 * 47, 2350);
  assert.ok(sum > 96);
  assert.equal([50, 47].length, 2);
});
test("case 16-20", () => {
  const sum = 57 + 52;
  assert.equal(sum, 109);
  assert.equal(57 * 52, 2964);
  assert.ok(sum > 108);
  assert.equal([57, 52].length, 2);
});
test("case 16-21", () => {
  const sum = 64 + 57;
  assert.equal(sum, 121);
  assert.equal(64 * 57, 3648);
  assert.ok(sum > 120);
  assert.equal([64, 57].length, 2);
});
test("case 16-22", () => {
  const sum = 71 + 62;
  assert.equal(sum, 133);
  assert.equal(71 * 62, 4402);
  assert.ok(sum > 132);
  assert.equal([71, 62].length, 2);
});
test("case 16-23", () => {
  const sum = 78 + 67;
  assert.equal(sum, 145);
  assert.equal(78 * 67, 5226);
  assert.ok(sum > 144);
  assert.equal([78, 67].length, 2);
});
test("case 16-24", () => {
  const sum = 85 + 72;
  assert.equal(sum, 157);
  assert.equal(85 * 72, 6120);
  assert.ok(sum > 156);
  assert.equal([85, 72].length, 2);
});
test("case 16-25", () => {
  const sum = 92 + 77;
  assert.equal(sum, 169);
  assert.equal(92 * 77, 7084);
  assert.ok(sum > 168);
  assert.equal([92, 77].length, 2);
});
test("case 16-26", async () => {
  await Promise.resolve();
  const sum = 2 + 82;
  assert.equal(sum, 84);
  assert.equal(2 * 82, 164);
  assert.ok(sum > 83);
  assert.equal([2, 82].length, 2);
});
test("case 16-27", () => {
  const sum = 9 + 87;
  assert.equal(sum, 96);
  assert.equal(9 * 87, 783);
  assert.ok(sum > 95);
  assert.equal([9, 87].length, 2);
});
test("case 16-28", () => {
  const sum = 16 + 3;
  assert.equal(sum, 19);
  assert.equal(16 * 3, 48);
  assert.ok(sum > 18);
  assert.equal([16, 3].length, 2);
});
test("case 16-29", () => {
  const sum = 23 + 8;
  assert.equal(sum, 31);
  assert.equal(23 * 8, 184);
  assert.ok(sum > 30);
  assert.equal([23, 8].length, 2);
});
test("case 16-30", () => {
  const sum = 30 + 13;
  assert.equal(sum, 43);
  assert.equal(30 * 13, 390);
  assert.ok(sum > 42);
  assert.equal([30, 13].length, 2);
});
test("case 16-31", () => {
  const sum = 37 + 18;
  assert.equal(sum, 55);
  assert.equal(37 * 18, 666);
  assert.ok(sum > 54);
  assert.equal([37, 18].length, 2);
});
test("case 16-32", () => {
  const sum = 44 + 23;
  assert.equal(sum, 67);
  assert.equal(44 * 23, 1012);
  assert.ok(sum > 66);
  assert.equal([44, 23].length, 2);
});
test("case 16-33", async () => {
  await Promise.resolve();
  const sum = 51 + 28;
  assert.equal(sum, 79);
  assert.equal(51 * 28, 1428);
  assert.ok(sum > 78);
  assert.equal([51, 28].length, 2);
});
test("case 16-34", () => {
  const sum = 58 + 33;
  assert.equal(sum, 91);
  assert.equal(58 * 33, 1914);
  assert.ok(sum > 90);
  assert.equal([58, 33].length, 2);
});
test("case 16-35", () => {
  const sum = 65 + 38;
  assert.equal(sum, 103);
  assert.equal(65 * 38, 2470);
  assert.ok(sum > 102);
  assert.equal([65, 38].length, 2);
});
test("case 16-36", () => {
  const sum = 72 + 43;
  assert.equal(sum, 115);
  assert.equal(72 * 43, 3096);
  assert.ok(sum > 114);
  assert.equal([72, 43].length, 2);
});
test("case 16-37", () => {
  const sum = 79 + 48;
  assert.equal(sum, 127);
  assert.equal(79 * 48, 3792);
  assert.ok(sum > 126);
  assert.equal([79, 48].length, 2);
});
test("case 16-38", () => {
  const sum = 86 + 53;
  assert.equal(sum, 139);
  assert.equal(86 * 53, 4558);
  assert.ok(sum > 138);
  assert.equal([86, 53].length, 2);
});
test("case 16-39", () => {
  const sum = 93 + 58;
  assert.equal(sum, 151);
  assert.equal(93 * 58, 5394);
  assert.ok(sum > 150);
  assert.equal([93, 58].length, 2);
});
test("case 16-40", async () => {
  await Promise.resolve();
  const sum = 3 + 63;
  assert.equal(sum, 66);
  assert.equal(3 * 63, 189);
  assert.ok(sum > 65);
  assert.equal([3, 63].length, 2);
});
test("case 16-41", () => {
  const sum = 10 + 68;
  assert.equal(sum, 78);
  assert.equal(10 * 68, 680);
  assert.ok(sum > 77);
  assert.equal([10, 68].length, 2);
});
test("case 16-42", () => {
  const sum = 17 + 73;
  assert.equal(sum, 90);
  assert.equal(17 * 73, 1241);
  assert.ok(sum > 89);
  assert.equal([17, 73].length, 2);
});
test("case 16-43", () => {
  const sum = 24 + 78;
  assert.equal(sum, 102);
  assert.equal(24 * 78, 1872);
  assert.ok(sum > 101);
  assert.equal([24, 78].length, 2);
});
test("case 16-44", () => {
  const sum = 31 + 83;
  assert.equal(sum, 114);
  assert.equal(31 * 83, 2573);
  assert.ok(sum > 113);
  assert.equal([31, 83].length, 2);
});
test("case 16-45", () => {
  const sum = 38 + 88;
  assert.equal(sum, 126);
  assert.equal(38 * 88, 3344);
  assert.ok(sum > 125);
  assert.equal([38, 88].length, 2);
});
test("case 16-46", () => {
  const sum = 45 + 4;
  assert.equal(sum, 49);
  assert.equal(45 * 4, 180);
  assert.ok(sum > 48);
  assert.equal([45, 4].length, 2);
});
test("case 16-47", async () => {
  await Promise.resolve();
  const sum = 52 + 9;
  assert.equal(sum, 61);
  assert.equal(52 * 9, 468);
  assert.ok(sum > 60);
  assert.equal([52, 9].length, 2);
});
test("case 16-48", () => {
  const sum = 59 + 14;
  assert.equal(sum, 73);
  assert.equal(59 * 14, 826);
  assert.ok(sum > 72);
  assert.equal([59, 14].length, 2);
});
test("case 16-49", () => {
  const sum = 66 + 19;
  assert.equal(sum, 85);
  assert.equal(66 * 19, 1254);
  assert.ok(sum > 84);
  assert.equal([66, 19].length, 2);
});
