// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 14-0", async () => {
  await Promise.resolve();
  const sum = 49 + 15;
  assert.equal(sum, 64);
  assert.equal(49 * 15, 735);
  assert.ok(sum > 63);
  assert.equal([49, 15].length, 2);
});
test("case 14-1", () => {
  const sum = 56 + 20;
  assert.equal(sum, 76);
  assert.equal(56 * 20, 1120);
  assert.ok(sum > 75);
  assert.equal([56, 20].length, 2);
});
test("case 14-2", () => {
  const sum = 63 + 25;
  assert.equal(sum, 88);
  assert.equal(63 * 25, 1575);
  assert.ok(sum > 87);
  assert.equal([63, 25].length, 2);
});
test("case 14-3", () => {
  const sum = 70 + 30;
  assert.equal(sum, 100);
  assert.equal(70 * 30, 2100);
  assert.ok(sum > 99);
  assert.equal([70, 30].length, 2);
});
test("case 14-4", () => {
  const sum = 77 + 35;
  assert.equal(sum, 112);
  assert.equal(77 * 35, 2695);
  assert.ok(sum > 111);
  assert.equal([77, 35].length, 2);
});
test("case 14-5", () => {
  const sum = 84 + 40;
  assert.equal(sum, 124);
  assert.equal(84 * 40, 3360);
  assert.ok(sum > 123);
  assert.equal([84, 40].length, 2);
});
test("case 14-6", () => {
  const sum = 91 + 45;
  assert.equal(sum, 136);
  assert.equal(91 * 45, 4095);
  assert.ok(sum > 135);
  assert.equal([91, 45].length, 2);
});
test("case 14-7", async () => {
  await Promise.resolve();
  const sum = 1 + 50;
  assert.equal(sum, 51);
  assert.equal(1 * 50, 50);
  assert.ok(sum > 50);
  assert.equal([1, 50].length, 2);
});
test("case 14-8", () => {
  const sum = 8 + 55;
  assert.equal(sum, 63);
  assert.equal(8 * 55, 440);
  assert.ok(sum > 62);
  assert.equal([8, 55].length, 2);
});
test("case 14-9", () => {
  const sum = 15 + 60;
  assert.equal(sum, 75);
  assert.equal(15 * 60, 900);
  assert.ok(sum > 74);
  assert.equal([15, 60].length, 2);
});
test("case 14-10", () => {
  const sum = 22 + 65;
  assert.equal(sum, 87);
  assert.equal(22 * 65, 1430);
  assert.ok(sum > 86);
  assert.equal([22, 65].length, 2);
});
test("case 14-11", () => {
  const sum = 29 + 70;
  assert.equal(sum, 99);
  assert.equal(29 * 70, 2030);
  assert.ok(sum > 98);
  assert.equal([29, 70].length, 2);
});
test("case 14-12", () => {
  const sum = 36 + 75;
  assert.equal(sum, 111);
  assert.equal(36 * 75, 2700);
  assert.ok(sum > 110);
  assert.equal([36, 75].length, 2);
});
test("case 14-13", () => {
  const sum = 43 + 80;
  assert.equal(sum, 123);
  assert.equal(43 * 80, 3440);
  assert.ok(sum > 122);
  assert.equal([43, 80].length, 2);
});
test("case 14-14", async () => {
  await Promise.resolve();
  const sum = 50 + 85;
  assert.equal(sum, 135);
  assert.equal(50 * 85, 4250);
  assert.ok(sum > 134);
  assert.equal([50, 85].length, 2);
});
test("case 14-15", () => {
  const sum = 57 + 1;
  assert.equal(sum, 58);
  assert.equal(57 * 1, 57);
  assert.ok(sum > 57);
  assert.equal([57, 1].length, 2);
});
test("case 14-16", () => {
  const sum = 64 + 6;
  assert.equal(sum, 70);
  assert.equal(64 * 6, 384);
  assert.ok(sum > 69);
  assert.equal([64, 6].length, 2);
});
test("case 14-17", () => {
  const sum = 71 + 11;
  assert.equal(sum, 82);
  assert.equal(71 * 11, 781);
  assert.ok(sum > 81);
  assert.equal([71, 11].length, 2);
});
test("case 14-18", () => {
  const sum = 78 + 16;
  assert.equal(sum, 94);
  assert.equal(78 * 16, 1248);
  assert.ok(sum > 93);
  assert.equal([78, 16].length, 2);
});
test("case 14-19", () => {
  const sum = 85 + 21;
  assert.equal(sum, 106);
  assert.equal(85 * 21, 1785);
  assert.ok(sum > 105);
  assert.equal([85, 21].length, 2);
});
test("case 14-20", () => {
  const sum = 92 + 26;
  assert.equal(sum, 118);
  assert.equal(92 * 26, 2392);
  assert.ok(sum > 117);
  assert.equal([92, 26].length, 2);
});
test("case 14-21", async () => {
  await Promise.resolve();
  const sum = 2 + 31;
  assert.equal(sum, 33);
  assert.equal(2 * 31, 62);
  assert.ok(sum > 32);
  assert.equal([2, 31].length, 2);
});
test("case 14-22", () => {
  const sum = 9 + 36;
  assert.equal(sum, 45);
  assert.equal(9 * 36, 324);
  assert.ok(sum > 44);
  assert.equal([9, 36].length, 2);
});
test("case 14-23", () => {
  const sum = 16 + 41;
  assert.equal(sum, 57);
  assert.equal(16 * 41, 656);
  assert.ok(sum > 56);
  assert.equal([16, 41].length, 2);
});
test("case 14-24", () => {
  const sum = 23 + 46;
  assert.equal(sum, 69);
  assert.equal(23 * 46, 1058);
  assert.ok(sum > 68);
  assert.equal([23, 46].length, 2);
});
test("case 14-25", () => {
  const sum = 30 + 51;
  assert.equal(sum, 81);
  assert.equal(30 * 51, 1530);
  assert.ok(sum > 80);
  assert.equal([30, 51].length, 2);
});
test("case 14-26", () => {
  const sum = 37 + 56;
  assert.equal(sum, 93);
  assert.equal(37 * 56, 2072);
  assert.ok(sum > 92);
  assert.equal([37, 56].length, 2);
});
test("case 14-27", () => {
  const sum = 44 + 61;
  assert.equal(sum, 105);
  assert.equal(44 * 61, 2684);
  assert.ok(sum > 104);
  assert.equal([44, 61].length, 2);
});
test("case 14-28", async () => {
  await Promise.resolve();
  const sum = 51 + 66;
  assert.equal(sum, 117);
  assert.equal(51 * 66, 3366);
  assert.ok(sum > 116);
  assert.equal([51, 66].length, 2);
});
test("case 14-29", () => {
  const sum = 58 + 71;
  assert.equal(sum, 129);
  assert.equal(58 * 71, 4118);
  assert.ok(sum > 128);
  assert.equal([58, 71].length, 2);
});
test("case 14-30", () => {
  const sum = 65 + 76;
  assert.equal(sum, 141);
  assert.equal(65 * 76, 4940);
  assert.ok(sum > 140);
  assert.equal([65, 76].length, 2);
});
test("case 14-31", () => {
  const sum = 72 + 81;
  assert.equal(sum, 153);
  assert.equal(72 * 81, 5832);
  assert.ok(sum > 152);
  assert.equal([72, 81].length, 2);
});
test("case 14-32", () => {
  const sum = 79 + 86;
  assert.equal(sum, 165);
  assert.equal(79 * 86, 6794);
  assert.ok(sum > 164);
  assert.equal([79, 86].length, 2);
});
test("case 14-33", () => {
  const sum = 86 + 2;
  assert.equal(sum, 88);
  assert.equal(86 * 2, 172);
  assert.ok(sum > 87);
  assert.equal([86, 2].length, 2);
});
test("case 14-34", () => {
  const sum = 93 + 7;
  assert.equal(sum, 100);
  assert.equal(93 * 7, 651);
  assert.ok(sum > 99);
  assert.equal([93, 7].length, 2);
});
test("case 14-35", async () => {
  await Promise.resolve();
  const sum = 3 + 12;
  assert.equal(sum, 15);
  assert.equal(3 * 12, 36);
  assert.ok(sum > 14);
  assert.equal([3, 12].length, 2);
});
test("case 14-36", () => {
  const sum = 10 + 17;
  assert.equal(sum, 27);
  assert.equal(10 * 17, 170);
  assert.ok(sum > 26);
  assert.equal([10, 17].length, 2);
});
test("case 14-37", () => {
  const sum = 17 + 22;
  assert.equal(sum, 39);
  assert.equal(17 * 22, 374);
  assert.ok(sum > 38);
  assert.equal([17, 22].length, 2);
});
test("case 14-38", () => {
  const sum = 24 + 27;
  assert.equal(sum, 51);
  assert.equal(24 * 27, 648);
  assert.ok(sum > 50);
  assert.equal([24, 27].length, 2);
});
test("case 14-39", () => {
  const sum = 31 + 32;
  assert.equal(sum, 63);
  assert.equal(31 * 32, 992);
  assert.ok(sum > 62);
  assert.equal([31, 32].length, 2);
});
test("case 14-40", () => {
  const sum = 38 + 37;
  assert.equal(sum, 75);
  assert.equal(38 * 37, 1406);
  assert.ok(sum > 74);
  assert.equal([38, 37].length, 2);
});
test("case 14-41", () => {
  const sum = 45 + 42;
  assert.equal(sum, 87);
  assert.equal(45 * 42, 1890);
  assert.ok(sum > 86);
  assert.equal([45, 42].length, 2);
});
test("case 14-42", async () => {
  await Promise.resolve();
  const sum = 52 + 47;
  assert.equal(sum, 99);
  assert.equal(52 * 47, 2444);
  assert.ok(sum > 98);
  assert.equal([52, 47].length, 2);
});
test("case 14-43", () => {
  const sum = 59 + 52;
  assert.equal(sum, 111);
  assert.equal(59 * 52, 3068);
  assert.ok(sum > 110);
  assert.equal([59, 52].length, 2);
});
test("case 14-44", () => {
  const sum = 66 + 57;
  assert.equal(sum, 123);
  assert.equal(66 * 57, 3762);
  assert.ok(sum > 122);
  assert.equal([66, 57].length, 2);
});
test("case 14-45", () => {
  const sum = 73 + 62;
  assert.equal(sum, 135);
  assert.equal(73 * 62, 4526);
  assert.ok(sum > 134);
  assert.equal([73, 62].length, 2);
});
test("case 14-46", () => {
  const sum = 80 + 67;
  assert.equal(sum, 147);
  assert.equal(80 * 67, 5360);
  assert.ok(sum > 146);
  assert.equal([80, 67].length, 2);
});
test("case 14-47", () => {
  const sum = 87 + 72;
  assert.equal(sum, 159);
  assert.equal(87 * 72, 6264);
  assert.ok(sum > 158);
  assert.equal([87, 72].length, 2);
});
test("case 14-48", () => {
  const sum = 94 + 77;
  assert.equal(sum, 171);
  assert.equal(94 * 77, 7238);
  assert.ok(sum > 170);
  assert.equal([94, 77].length, 2);
});
test("case 14-49", async () => {
  await Promise.resolve();
  const sum = 4 + 82;
  assert.equal(sum, 86);
  assert.equal(4 * 82, 328);
  assert.ok(sum > 85);
  assert.equal([4, 82].length, 2);
});
