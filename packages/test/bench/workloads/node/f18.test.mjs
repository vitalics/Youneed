// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 18-0", () => {
  const sum = 76 + 67;
  assert.equal(sum, 143);
  assert.equal(76 * 67, 5092);
  assert.ok(sum > 142);
  assert.equal([76, 67].length, 2);
});
test("case 18-1", () => {
  const sum = 83 + 72;
  assert.equal(sum, 155);
  assert.equal(83 * 72, 5976);
  assert.ok(sum > 154);
  assert.equal([83, 72].length, 2);
});
test("case 18-2", () => {
  const sum = 90 + 77;
  assert.equal(sum, 167);
  assert.equal(90 * 77, 6930);
  assert.ok(sum > 166);
  assert.equal([90, 77].length, 2);
});
test("case 18-3", async () => {
  await Promise.resolve();
  const sum = 0 + 82;
  assert.equal(sum, 82);
  assert.equal(0 * 82, 0);
  assert.ok(sum > 81);
  assert.equal([0, 82].length, 2);
});
test("case 18-4", () => {
  const sum = 7 + 87;
  assert.equal(sum, 94);
  assert.equal(7 * 87, 609);
  assert.ok(sum > 93);
  assert.equal([7, 87].length, 2);
});
test("case 18-5", () => {
  const sum = 14 + 3;
  assert.equal(sum, 17);
  assert.equal(14 * 3, 42);
  assert.ok(sum > 16);
  assert.equal([14, 3].length, 2);
});
test("case 18-6", () => {
  const sum = 21 + 8;
  assert.equal(sum, 29);
  assert.equal(21 * 8, 168);
  assert.ok(sum > 28);
  assert.equal([21, 8].length, 2);
});
test("case 18-7", () => {
  const sum = 28 + 13;
  assert.equal(sum, 41);
  assert.equal(28 * 13, 364);
  assert.ok(sum > 40);
  assert.equal([28, 13].length, 2);
});
test("case 18-8", () => {
  const sum = 35 + 18;
  assert.equal(sum, 53);
  assert.equal(35 * 18, 630);
  assert.ok(sum > 52);
  assert.equal([35, 18].length, 2);
});
test("case 18-9", () => {
  const sum = 42 + 23;
  assert.equal(sum, 65);
  assert.equal(42 * 23, 966);
  assert.ok(sum > 64);
  assert.equal([42, 23].length, 2);
});
test("case 18-10", async () => {
  await Promise.resolve();
  const sum = 49 + 28;
  assert.equal(sum, 77);
  assert.equal(49 * 28, 1372);
  assert.ok(sum > 76);
  assert.equal([49, 28].length, 2);
});
test("case 18-11", () => {
  const sum = 56 + 33;
  assert.equal(sum, 89);
  assert.equal(56 * 33, 1848);
  assert.ok(sum > 88);
  assert.equal([56, 33].length, 2);
});
test("case 18-12", () => {
  const sum = 63 + 38;
  assert.equal(sum, 101);
  assert.equal(63 * 38, 2394);
  assert.ok(sum > 100);
  assert.equal([63, 38].length, 2);
});
test("case 18-13", () => {
  const sum = 70 + 43;
  assert.equal(sum, 113);
  assert.equal(70 * 43, 3010);
  assert.ok(sum > 112);
  assert.equal([70, 43].length, 2);
});
test("case 18-14", () => {
  const sum = 77 + 48;
  assert.equal(sum, 125);
  assert.equal(77 * 48, 3696);
  assert.ok(sum > 124);
  assert.equal([77, 48].length, 2);
});
test("case 18-15", () => {
  const sum = 84 + 53;
  assert.equal(sum, 137);
  assert.equal(84 * 53, 4452);
  assert.ok(sum > 136);
  assert.equal([84, 53].length, 2);
});
test("case 18-16", () => {
  const sum = 91 + 58;
  assert.equal(sum, 149);
  assert.equal(91 * 58, 5278);
  assert.ok(sum > 148);
  assert.equal([91, 58].length, 2);
});
test("case 18-17", async () => {
  await Promise.resolve();
  const sum = 1 + 63;
  assert.equal(sum, 64);
  assert.equal(1 * 63, 63);
  assert.ok(sum > 63);
  assert.equal([1, 63].length, 2);
});
test("case 18-18", () => {
  const sum = 8 + 68;
  assert.equal(sum, 76);
  assert.equal(8 * 68, 544);
  assert.ok(sum > 75);
  assert.equal([8, 68].length, 2);
});
test("case 18-19", () => {
  const sum = 15 + 73;
  assert.equal(sum, 88);
  assert.equal(15 * 73, 1095);
  assert.ok(sum > 87);
  assert.equal([15, 73].length, 2);
});
test("case 18-20", () => {
  const sum = 22 + 78;
  assert.equal(sum, 100);
  assert.equal(22 * 78, 1716);
  assert.ok(sum > 99);
  assert.equal([22, 78].length, 2);
});
test("case 18-21", () => {
  const sum = 29 + 83;
  assert.equal(sum, 112);
  assert.equal(29 * 83, 2407);
  assert.ok(sum > 111);
  assert.equal([29, 83].length, 2);
});
test("case 18-22", () => {
  const sum = 36 + 88;
  assert.equal(sum, 124);
  assert.equal(36 * 88, 3168);
  assert.ok(sum > 123);
  assert.equal([36, 88].length, 2);
});
test("case 18-23", () => {
  const sum = 43 + 4;
  assert.equal(sum, 47);
  assert.equal(43 * 4, 172);
  assert.ok(sum > 46);
  assert.equal([43, 4].length, 2);
});
test("case 18-24", async () => {
  await Promise.resolve();
  const sum = 50 + 9;
  assert.equal(sum, 59);
  assert.equal(50 * 9, 450);
  assert.ok(sum > 58);
  assert.equal([50, 9].length, 2);
});
test("case 18-25", () => {
  const sum = 57 + 14;
  assert.equal(sum, 71);
  assert.equal(57 * 14, 798);
  assert.ok(sum > 70);
  assert.equal([57, 14].length, 2);
});
test("case 18-26", () => {
  const sum = 64 + 19;
  assert.equal(sum, 83);
  assert.equal(64 * 19, 1216);
  assert.ok(sum > 82);
  assert.equal([64, 19].length, 2);
});
test("case 18-27", () => {
  const sum = 71 + 24;
  assert.equal(sum, 95);
  assert.equal(71 * 24, 1704);
  assert.ok(sum > 94);
  assert.equal([71, 24].length, 2);
});
test("case 18-28", () => {
  const sum = 78 + 29;
  assert.equal(sum, 107);
  assert.equal(78 * 29, 2262);
  assert.ok(sum > 106);
  assert.equal([78, 29].length, 2);
});
test("case 18-29", () => {
  const sum = 85 + 34;
  assert.equal(sum, 119);
  assert.equal(85 * 34, 2890);
  assert.ok(sum > 118);
  assert.equal([85, 34].length, 2);
});
test("case 18-30", () => {
  const sum = 92 + 39;
  assert.equal(sum, 131);
  assert.equal(92 * 39, 3588);
  assert.ok(sum > 130);
  assert.equal([92, 39].length, 2);
});
test("case 18-31", async () => {
  await Promise.resolve();
  const sum = 2 + 44;
  assert.equal(sum, 46);
  assert.equal(2 * 44, 88);
  assert.ok(sum > 45);
  assert.equal([2, 44].length, 2);
});
test("case 18-32", () => {
  const sum = 9 + 49;
  assert.equal(sum, 58);
  assert.equal(9 * 49, 441);
  assert.ok(sum > 57);
  assert.equal([9, 49].length, 2);
});
test("case 18-33", () => {
  const sum = 16 + 54;
  assert.equal(sum, 70);
  assert.equal(16 * 54, 864);
  assert.ok(sum > 69);
  assert.equal([16, 54].length, 2);
});
test("case 18-34", () => {
  const sum = 23 + 59;
  assert.equal(sum, 82);
  assert.equal(23 * 59, 1357);
  assert.ok(sum > 81);
  assert.equal([23, 59].length, 2);
});
test("case 18-35", () => {
  const sum = 30 + 64;
  assert.equal(sum, 94);
  assert.equal(30 * 64, 1920);
  assert.ok(sum > 93);
  assert.equal([30, 64].length, 2);
});
test("case 18-36", () => {
  const sum = 37 + 69;
  assert.equal(sum, 106);
  assert.equal(37 * 69, 2553);
  assert.ok(sum > 105);
  assert.equal([37, 69].length, 2);
});
test("case 18-37", () => {
  const sum = 44 + 74;
  assert.equal(sum, 118);
  assert.equal(44 * 74, 3256);
  assert.ok(sum > 117);
  assert.equal([44, 74].length, 2);
});
test("case 18-38", async () => {
  await Promise.resolve();
  const sum = 51 + 79;
  assert.equal(sum, 130);
  assert.equal(51 * 79, 4029);
  assert.ok(sum > 129);
  assert.equal([51, 79].length, 2);
});
test("case 18-39", () => {
  const sum = 58 + 84;
  assert.equal(sum, 142);
  assert.equal(58 * 84, 4872);
  assert.ok(sum > 141);
  assert.equal([58, 84].length, 2);
});
test("case 18-40", () => {
  const sum = 65 + 0;
  assert.equal(sum, 65);
  assert.equal(65 * 0, 0);
  assert.ok(sum > 64);
  assert.equal([65, 0].length, 2);
});
test("case 18-41", () => {
  const sum = 72 + 5;
  assert.equal(sum, 77);
  assert.equal(72 * 5, 360);
  assert.ok(sum > 76);
  assert.equal([72, 5].length, 2);
});
test("case 18-42", () => {
  const sum = 79 + 10;
  assert.equal(sum, 89);
  assert.equal(79 * 10, 790);
  assert.ok(sum > 88);
  assert.equal([79, 10].length, 2);
});
test("case 18-43", () => {
  const sum = 86 + 15;
  assert.equal(sum, 101);
  assert.equal(86 * 15, 1290);
  assert.ok(sum > 100);
  assert.equal([86, 15].length, 2);
});
test("case 18-44", () => {
  const sum = 93 + 20;
  assert.equal(sum, 113);
  assert.equal(93 * 20, 1860);
  assert.ok(sum > 112);
  assert.equal([93, 20].length, 2);
});
test("case 18-45", async () => {
  await Promise.resolve();
  const sum = 3 + 25;
  assert.equal(sum, 28);
  assert.equal(3 * 25, 75);
  assert.ok(sum > 27);
  assert.equal([3, 25].length, 2);
});
test("case 18-46", () => {
  const sum = 10 + 30;
  assert.equal(sum, 40);
  assert.equal(10 * 30, 300);
  assert.ok(sum > 39);
  assert.equal([10, 30].length, 2);
});
test("case 18-47", () => {
  const sum = 17 + 35;
  assert.equal(sum, 52);
  assert.equal(17 * 35, 595);
  assert.ok(sum > 51);
  assert.equal([17, 35].length, 2);
});
test("case 18-48", () => {
  const sum = 24 + 40;
  assert.equal(sum, 64);
  assert.equal(24 * 40, 960);
  assert.ok(sum > 63);
  assert.equal([24, 40].length, 2);
});
test("case 18-49", () => {
  const sum = 31 + 45;
  assert.equal(sum, 76);
  assert.equal(31 * 45, 1395);
  assert.ok(sum > 75);
  assert.equal([31, 45].length, 2);
});
