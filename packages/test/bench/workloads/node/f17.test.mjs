// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 17-0", () => {
  const sum = 45 + 54;
  assert.equal(sum, 99);
  assert.equal(45 * 54, 2430);
  assert.ok(sum > 98);
  assert.equal([45, 54].length, 2);
});
test("case 17-1", () => {
  const sum = 52 + 59;
  assert.equal(sum, 111);
  assert.equal(52 * 59, 3068);
  assert.ok(sum > 110);
  assert.equal([52, 59].length, 2);
});
test("case 17-2", () => {
  const sum = 59 + 64;
  assert.equal(sum, 123);
  assert.equal(59 * 64, 3776);
  assert.ok(sum > 122);
  assert.equal([59, 64].length, 2);
});
test("case 17-3", () => {
  const sum = 66 + 69;
  assert.equal(sum, 135);
  assert.equal(66 * 69, 4554);
  assert.ok(sum > 134);
  assert.equal([66, 69].length, 2);
});
test("case 17-4", async () => {
  await Promise.resolve();
  const sum = 73 + 74;
  assert.equal(sum, 147);
  assert.equal(73 * 74, 5402);
  assert.ok(sum > 146);
  assert.equal([73, 74].length, 2);
});
test("case 17-5", () => {
  const sum = 80 + 79;
  assert.equal(sum, 159);
  assert.equal(80 * 79, 6320);
  assert.ok(sum > 158);
  assert.equal([80, 79].length, 2);
});
test("case 17-6", () => {
  const sum = 87 + 84;
  assert.equal(sum, 171);
  assert.equal(87 * 84, 7308);
  assert.ok(sum > 170);
  assert.equal([87, 84].length, 2);
});
test("case 17-7", () => {
  const sum = 94 + 0;
  assert.equal(sum, 94);
  assert.equal(94 * 0, 0);
  assert.ok(sum > 93);
  assert.equal([94, 0].length, 2);
});
test("case 17-8", () => {
  const sum = 4 + 5;
  assert.equal(sum, 9);
  assert.equal(4 * 5, 20);
  assert.ok(sum > 8);
  assert.equal([4, 5].length, 2);
});
test("case 17-9", () => {
  const sum = 11 + 10;
  assert.equal(sum, 21);
  assert.equal(11 * 10, 110);
  assert.ok(sum > 20);
  assert.equal([11, 10].length, 2);
});
test("case 17-10", () => {
  const sum = 18 + 15;
  assert.equal(sum, 33);
  assert.equal(18 * 15, 270);
  assert.ok(sum > 32);
  assert.equal([18, 15].length, 2);
});
test("case 17-11", async () => {
  await Promise.resolve();
  const sum = 25 + 20;
  assert.equal(sum, 45);
  assert.equal(25 * 20, 500);
  assert.ok(sum > 44);
  assert.equal([25, 20].length, 2);
});
test("case 17-12", () => {
  const sum = 32 + 25;
  assert.equal(sum, 57);
  assert.equal(32 * 25, 800);
  assert.ok(sum > 56);
  assert.equal([32, 25].length, 2);
});
test("case 17-13", () => {
  const sum = 39 + 30;
  assert.equal(sum, 69);
  assert.equal(39 * 30, 1170);
  assert.ok(sum > 68);
  assert.equal([39, 30].length, 2);
});
test("case 17-14", () => {
  const sum = 46 + 35;
  assert.equal(sum, 81);
  assert.equal(46 * 35, 1610);
  assert.ok(sum > 80);
  assert.equal([46, 35].length, 2);
});
test("case 17-15", () => {
  const sum = 53 + 40;
  assert.equal(sum, 93);
  assert.equal(53 * 40, 2120);
  assert.ok(sum > 92);
  assert.equal([53, 40].length, 2);
});
test("case 17-16", () => {
  const sum = 60 + 45;
  assert.equal(sum, 105);
  assert.equal(60 * 45, 2700);
  assert.ok(sum > 104);
  assert.equal([60, 45].length, 2);
});
test("case 17-17", () => {
  const sum = 67 + 50;
  assert.equal(sum, 117);
  assert.equal(67 * 50, 3350);
  assert.ok(sum > 116);
  assert.equal([67, 50].length, 2);
});
test("case 17-18", async () => {
  await Promise.resolve();
  const sum = 74 + 55;
  assert.equal(sum, 129);
  assert.equal(74 * 55, 4070);
  assert.ok(sum > 128);
  assert.equal([74, 55].length, 2);
});
test("case 17-19", () => {
  const sum = 81 + 60;
  assert.equal(sum, 141);
  assert.equal(81 * 60, 4860);
  assert.ok(sum > 140);
  assert.equal([81, 60].length, 2);
});
test("case 17-20", () => {
  const sum = 88 + 65;
  assert.equal(sum, 153);
  assert.equal(88 * 65, 5720);
  assert.ok(sum > 152);
  assert.equal([88, 65].length, 2);
});
test("case 17-21", () => {
  const sum = 95 + 70;
  assert.equal(sum, 165);
  assert.equal(95 * 70, 6650);
  assert.ok(sum > 164);
  assert.equal([95, 70].length, 2);
});
test("case 17-22", () => {
  const sum = 5 + 75;
  assert.equal(sum, 80);
  assert.equal(5 * 75, 375);
  assert.ok(sum > 79);
  assert.equal([5, 75].length, 2);
});
test("case 17-23", () => {
  const sum = 12 + 80;
  assert.equal(sum, 92);
  assert.equal(12 * 80, 960);
  assert.ok(sum > 91);
  assert.equal([12, 80].length, 2);
});
test("case 17-24", () => {
  const sum = 19 + 85;
  assert.equal(sum, 104);
  assert.equal(19 * 85, 1615);
  assert.ok(sum > 103);
  assert.equal([19, 85].length, 2);
});
test("case 17-25", async () => {
  await Promise.resolve();
  const sum = 26 + 1;
  assert.equal(sum, 27);
  assert.equal(26 * 1, 26);
  assert.ok(sum > 26);
  assert.equal([26, 1].length, 2);
});
test("case 17-26", () => {
  const sum = 33 + 6;
  assert.equal(sum, 39);
  assert.equal(33 * 6, 198);
  assert.ok(sum > 38);
  assert.equal([33, 6].length, 2);
});
test("case 17-27", () => {
  const sum = 40 + 11;
  assert.equal(sum, 51);
  assert.equal(40 * 11, 440);
  assert.ok(sum > 50);
  assert.equal([40, 11].length, 2);
});
test("case 17-28", () => {
  const sum = 47 + 16;
  assert.equal(sum, 63);
  assert.equal(47 * 16, 752);
  assert.ok(sum > 62);
  assert.equal([47, 16].length, 2);
});
test("case 17-29", () => {
  const sum = 54 + 21;
  assert.equal(sum, 75);
  assert.equal(54 * 21, 1134);
  assert.ok(sum > 74);
  assert.equal([54, 21].length, 2);
});
test("case 17-30", () => {
  const sum = 61 + 26;
  assert.equal(sum, 87);
  assert.equal(61 * 26, 1586);
  assert.ok(sum > 86);
  assert.equal([61, 26].length, 2);
});
test("case 17-31", () => {
  const sum = 68 + 31;
  assert.equal(sum, 99);
  assert.equal(68 * 31, 2108);
  assert.ok(sum > 98);
  assert.equal([68, 31].length, 2);
});
test("case 17-32", async () => {
  await Promise.resolve();
  const sum = 75 + 36;
  assert.equal(sum, 111);
  assert.equal(75 * 36, 2700);
  assert.ok(sum > 110);
  assert.equal([75, 36].length, 2);
});
test("case 17-33", () => {
  const sum = 82 + 41;
  assert.equal(sum, 123);
  assert.equal(82 * 41, 3362);
  assert.ok(sum > 122);
  assert.equal([82, 41].length, 2);
});
test("case 17-34", () => {
  const sum = 89 + 46;
  assert.equal(sum, 135);
  assert.equal(89 * 46, 4094);
  assert.ok(sum > 134);
  assert.equal([89, 46].length, 2);
});
test("case 17-35", () => {
  const sum = 96 + 51;
  assert.equal(sum, 147);
  assert.equal(96 * 51, 4896);
  assert.ok(sum > 146);
  assert.equal([96, 51].length, 2);
});
test("case 17-36", () => {
  const sum = 6 + 56;
  assert.equal(sum, 62);
  assert.equal(6 * 56, 336);
  assert.ok(sum > 61);
  assert.equal([6, 56].length, 2);
});
test("case 17-37", () => {
  const sum = 13 + 61;
  assert.equal(sum, 74);
  assert.equal(13 * 61, 793);
  assert.ok(sum > 73);
  assert.equal([13, 61].length, 2);
});
test("case 17-38", () => {
  const sum = 20 + 66;
  assert.equal(sum, 86);
  assert.equal(20 * 66, 1320);
  assert.ok(sum > 85);
  assert.equal([20, 66].length, 2);
});
test("case 17-39", async () => {
  await Promise.resolve();
  const sum = 27 + 71;
  assert.equal(sum, 98);
  assert.equal(27 * 71, 1917);
  assert.ok(sum > 97);
  assert.equal([27, 71].length, 2);
});
test("case 17-40", () => {
  const sum = 34 + 76;
  assert.equal(sum, 110);
  assert.equal(34 * 76, 2584);
  assert.ok(sum > 109);
  assert.equal([34, 76].length, 2);
});
test("case 17-41", () => {
  const sum = 41 + 81;
  assert.equal(sum, 122);
  assert.equal(41 * 81, 3321);
  assert.ok(sum > 121);
  assert.equal([41, 81].length, 2);
});
test("case 17-42", () => {
  const sum = 48 + 86;
  assert.equal(sum, 134);
  assert.equal(48 * 86, 4128);
  assert.ok(sum > 133);
  assert.equal([48, 86].length, 2);
});
test("case 17-43", () => {
  const sum = 55 + 2;
  assert.equal(sum, 57);
  assert.equal(55 * 2, 110);
  assert.ok(sum > 56);
  assert.equal([55, 2].length, 2);
});
test("case 17-44", () => {
  const sum = 62 + 7;
  assert.equal(sum, 69);
  assert.equal(62 * 7, 434);
  assert.ok(sum > 68);
  assert.equal([62, 7].length, 2);
});
test("case 17-45", () => {
  const sum = 69 + 12;
  assert.equal(sum, 81);
  assert.equal(69 * 12, 828);
  assert.ok(sum > 80);
  assert.equal([69, 12].length, 2);
});
test("case 17-46", async () => {
  await Promise.resolve();
  const sum = 76 + 17;
  assert.equal(sum, 93);
  assert.equal(76 * 17, 1292);
  assert.ok(sum > 92);
  assert.equal([76, 17].length, 2);
});
test("case 17-47", () => {
  const sum = 83 + 22;
  assert.equal(sum, 105);
  assert.equal(83 * 22, 1826);
  assert.ok(sum > 104);
  assert.equal([83, 22].length, 2);
});
test("case 17-48", () => {
  const sum = 90 + 27;
  assert.equal(sum, 117);
  assert.equal(90 * 27, 2430);
  assert.ok(sum > 116);
  assert.equal([90, 27].length, 2);
});
test("case 17-49", () => {
  const sum = 0 + 32;
  assert.equal(sum, 32);
  assert.equal(0 * 32, 0);
  assert.ok(sum > 31);
  assert.equal([0, 32].length, 2);
});
