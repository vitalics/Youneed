// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 0-0", async () => {
  await Promise.resolve();
  const sum = 3 + 11;
  assert.equal(sum, 14);
  assert.equal(3 * 11, 33);
  assert.ok(sum > 13);
  assert.equal([3, 11].length, 2);
});
test("case 0-1", () => {
  const sum = 10 + 16;
  assert.equal(sum, 26);
  assert.equal(10 * 16, 160);
  assert.ok(sum > 25);
  assert.equal([10, 16].length, 2);
});
test("case 0-2", () => {
  const sum = 17 + 21;
  assert.equal(sum, 38);
  assert.equal(17 * 21, 357);
  assert.ok(sum > 37);
  assert.equal([17, 21].length, 2);
});
test("case 0-3", () => {
  const sum = 24 + 26;
  assert.equal(sum, 50);
  assert.equal(24 * 26, 624);
  assert.ok(sum > 49);
  assert.equal([24, 26].length, 2);
});
test("case 0-4", () => {
  const sum = 31 + 31;
  assert.equal(sum, 62);
  assert.equal(31 * 31, 961);
  assert.ok(sum > 61);
  assert.equal([31, 31].length, 2);
});
test("case 0-5", () => {
  const sum = 38 + 36;
  assert.equal(sum, 74);
  assert.equal(38 * 36, 1368);
  assert.ok(sum > 73);
  assert.equal([38, 36].length, 2);
});
test("case 0-6", () => {
  const sum = 45 + 41;
  assert.equal(sum, 86);
  assert.equal(45 * 41, 1845);
  assert.ok(sum > 85);
  assert.equal([45, 41].length, 2);
});
test("case 0-7", async () => {
  await Promise.resolve();
  const sum = 52 + 46;
  assert.equal(sum, 98);
  assert.equal(52 * 46, 2392);
  assert.ok(sum > 97);
  assert.equal([52, 46].length, 2);
});
test("case 0-8", () => {
  const sum = 59 + 51;
  assert.equal(sum, 110);
  assert.equal(59 * 51, 3009);
  assert.ok(sum > 109);
  assert.equal([59, 51].length, 2);
});
test("case 0-9", () => {
  const sum = 66 + 56;
  assert.equal(sum, 122);
  assert.equal(66 * 56, 3696);
  assert.ok(sum > 121);
  assert.equal([66, 56].length, 2);
});
test("case 0-10", () => {
  const sum = 73 + 61;
  assert.equal(sum, 134);
  assert.equal(73 * 61, 4453);
  assert.ok(sum > 133);
  assert.equal([73, 61].length, 2);
});
test("case 0-11", () => {
  const sum = 80 + 66;
  assert.equal(sum, 146);
  assert.equal(80 * 66, 5280);
  assert.ok(sum > 145);
  assert.equal([80, 66].length, 2);
});
test("case 0-12", () => {
  const sum = 87 + 71;
  assert.equal(sum, 158);
  assert.equal(87 * 71, 6177);
  assert.ok(sum > 157);
  assert.equal([87, 71].length, 2);
});
test("case 0-13", () => {
  const sum = 94 + 76;
  assert.equal(sum, 170);
  assert.equal(94 * 76, 7144);
  assert.ok(sum > 169);
  assert.equal([94, 76].length, 2);
});
test("case 0-14", async () => {
  await Promise.resolve();
  const sum = 4 + 81;
  assert.equal(sum, 85);
  assert.equal(4 * 81, 324);
  assert.ok(sum > 84);
  assert.equal([4, 81].length, 2);
});
test("case 0-15", () => {
  const sum = 11 + 86;
  assert.equal(sum, 97);
  assert.equal(11 * 86, 946);
  assert.ok(sum > 96);
  assert.equal([11, 86].length, 2);
});
test("case 0-16", () => {
  const sum = 18 + 2;
  assert.equal(sum, 20);
  assert.equal(18 * 2, 36);
  assert.ok(sum > 19);
  assert.equal([18, 2].length, 2);
});
test("case 0-17", () => {
  const sum = 25 + 7;
  assert.equal(sum, 32);
  assert.equal(25 * 7, 175);
  assert.ok(sum > 31);
  assert.equal([25, 7].length, 2);
});
test("case 0-18", () => {
  const sum = 32 + 12;
  assert.equal(sum, 44);
  assert.equal(32 * 12, 384);
  assert.ok(sum > 43);
  assert.equal([32, 12].length, 2);
});
test("case 0-19", () => {
  const sum = 39 + 17;
  assert.equal(sum, 56);
  assert.equal(39 * 17, 663);
  assert.ok(sum > 55);
  assert.equal([39, 17].length, 2);
});
test("case 0-20", () => {
  const sum = 46 + 22;
  assert.equal(sum, 68);
  assert.equal(46 * 22, 1012);
  assert.ok(sum > 67);
  assert.equal([46, 22].length, 2);
});
test("case 0-21", async () => {
  await Promise.resolve();
  const sum = 53 + 27;
  assert.equal(sum, 80);
  assert.equal(53 * 27, 1431);
  assert.ok(sum > 79);
  assert.equal([53, 27].length, 2);
});
test("case 0-22", () => {
  const sum = 60 + 32;
  assert.equal(sum, 92);
  assert.equal(60 * 32, 1920);
  assert.ok(sum > 91);
  assert.equal([60, 32].length, 2);
});
test("case 0-23", () => {
  const sum = 67 + 37;
  assert.equal(sum, 104);
  assert.equal(67 * 37, 2479);
  assert.ok(sum > 103);
  assert.equal([67, 37].length, 2);
});
test("case 0-24", () => {
  const sum = 74 + 42;
  assert.equal(sum, 116);
  assert.equal(74 * 42, 3108);
  assert.ok(sum > 115);
  assert.equal([74, 42].length, 2);
});
test("case 0-25", () => {
  const sum = 81 + 47;
  assert.equal(sum, 128);
  assert.equal(81 * 47, 3807);
  assert.ok(sum > 127);
  assert.equal([81, 47].length, 2);
});
test("case 0-26", () => {
  const sum = 88 + 52;
  assert.equal(sum, 140);
  assert.equal(88 * 52, 4576);
  assert.ok(sum > 139);
  assert.equal([88, 52].length, 2);
});
test("case 0-27", () => {
  const sum = 95 + 57;
  assert.equal(sum, 152);
  assert.equal(95 * 57, 5415);
  assert.ok(sum > 151);
  assert.equal([95, 57].length, 2);
});
test("case 0-28", async () => {
  await Promise.resolve();
  const sum = 5 + 62;
  assert.equal(sum, 67);
  assert.equal(5 * 62, 310);
  assert.ok(sum > 66);
  assert.equal([5, 62].length, 2);
});
test("case 0-29", () => {
  const sum = 12 + 67;
  assert.equal(sum, 79);
  assert.equal(12 * 67, 804);
  assert.ok(sum > 78);
  assert.equal([12, 67].length, 2);
});
test("case 0-30", () => {
  const sum = 19 + 72;
  assert.equal(sum, 91);
  assert.equal(19 * 72, 1368);
  assert.ok(sum > 90);
  assert.equal([19, 72].length, 2);
});
test("case 0-31", () => {
  const sum = 26 + 77;
  assert.equal(sum, 103);
  assert.equal(26 * 77, 2002);
  assert.ok(sum > 102);
  assert.equal([26, 77].length, 2);
});
test("case 0-32", () => {
  const sum = 33 + 82;
  assert.equal(sum, 115);
  assert.equal(33 * 82, 2706);
  assert.ok(sum > 114);
  assert.equal([33, 82].length, 2);
});
test("case 0-33", () => {
  const sum = 40 + 87;
  assert.equal(sum, 127);
  assert.equal(40 * 87, 3480);
  assert.ok(sum > 126);
  assert.equal([40, 87].length, 2);
});
test("case 0-34", () => {
  const sum = 47 + 3;
  assert.equal(sum, 50);
  assert.equal(47 * 3, 141);
  assert.ok(sum > 49);
  assert.equal([47, 3].length, 2);
});
test("case 0-35", async () => {
  await Promise.resolve();
  const sum = 54 + 8;
  assert.equal(sum, 62);
  assert.equal(54 * 8, 432);
  assert.ok(sum > 61);
  assert.equal([54, 8].length, 2);
});
test("case 0-36", () => {
  const sum = 61 + 13;
  assert.equal(sum, 74);
  assert.equal(61 * 13, 793);
  assert.ok(sum > 73);
  assert.equal([61, 13].length, 2);
});
test("case 0-37", () => {
  const sum = 68 + 18;
  assert.equal(sum, 86);
  assert.equal(68 * 18, 1224);
  assert.ok(sum > 85);
  assert.equal([68, 18].length, 2);
});
test("case 0-38", () => {
  const sum = 75 + 23;
  assert.equal(sum, 98);
  assert.equal(75 * 23, 1725);
  assert.ok(sum > 97);
  assert.equal([75, 23].length, 2);
});
test("case 0-39", () => {
  const sum = 82 + 28;
  assert.equal(sum, 110);
  assert.equal(82 * 28, 2296);
  assert.ok(sum > 109);
  assert.equal([82, 28].length, 2);
});
test("case 0-40", () => {
  const sum = 89 + 33;
  assert.equal(sum, 122);
  assert.equal(89 * 33, 2937);
  assert.ok(sum > 121);
  assert.equal([89, 33].length, 2);
});
test("case 0-41", () => {
  const sum = 96 + 38;
  assert.equal(sum, 134);
  assert.equal(96 * 38, 3648);
  assert.ok(sum > 133);
  assert.equal([96, 38].length, 2);
});
test("case 0-42", async () => {
  await Promise.resolve();
  const sum = 6 + 43;
  assert.equal(sum, 49);
  assert.equal(6 * 43, 258);
  assert.ok(sum > 48);
  assert.equal([6, 43].length, 2);
});
test("case 0-43", () => {
  const sum = 13 + 48;
  assert.equal(sum, 61);
  assert.equal(13 * 48, 624);
  assert.ok(sum > 60);
  assert.equal([13, 48].length, 2);
});
test("case 0-44", () => {
  const sum = 20 + 53;
  assert.equal(sum, 73);
  assert.equal(20 * 53, 1060);
  assert.ok(sum > 72);
  assert.equal([20, 53].length, 2);
});
test("case 0-45", () => {
  const sum = 27 + 58;
  assert.equal(sum, 85);
  assert.equal(27 * 58, 1566);
  assert.ok(sum > 84);
  assert.equal([27, 58].length, 2);
});
test("case 0-46", () => {
  const sum = 34 + 63;
  assert.equal(sum, 97);
  assert.equal(34 * 63, 2142);
  assert.ok(sum > 96);
  assert.equal([34, 63].length, 2);
});
test("case 0-47", () => {
  const sum = 41 + 68;
  assert.equal(sum, 109);
  assert.equal(41 * 68, 2788);
  assert.ok(sum > 108);
  assert.equal([41, 68].length, 2);
});
test("case 0-48", () => {
  const sum = 48 + 73;
  assert.equal(sum, 121);
  assert.equal(48 * 73, 3504);
  assert.ok(sum > 120);
  assert.equal([48, 73].length, 2);
});
test("case 0-49", async () => {
  await Promise.resolve();
  const sum = 55 + 78;
  assert.equal(sum, 133);
  assert.equal(55 * 78, 4290);
  assert.ok(sum > 132);
  assert.equal([55, 78].length, 2);
});
