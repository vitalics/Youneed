// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 8-0", () => {
  const sum = 57 + 26;
  assert.equal(sum, 83);
  assert.equal(57 * 26, 1482);
  assert.ok(sum > 82);
  assert.equal([57, 26].length, 2);
});
test("case 8-1", () => {
  const sum = 64 + 31;
  assert.equal(sum, 95);
  assert.equal(64 * 31, 1984);
  assert.ok(sum > 94);
  assert.equal([64, 31].length, 2);
});
test("case 8-2", () => {
  const sum = 71 + 36;
  assert.equal(sum, 107);
  assert.equal(71 * 36, 2556);
  assert.ok(sum > 106);
  assert.equal([71, 36].length, 2);
});
test("case 8-3", () => {
  const sum = 78 + 41;
  assert.equal(sum, 119);
  assert.equal(78 * 41, 3198);
  assert.ok(sum > 118);
  assert.equal([78, 41].length, 2);
});
test("case 8-4", () => {
  const sum = 85 + 46;
  assert.equal(sum, 131);
  assert.equal(85 * 46, 3910);
  assert.ok(sum > 130);
  assert.equal([85, 46].length, 2);
});
test("case 8-5", () => {
  const sum = 92 + 51;
  assert.equal(sum, 143);
  assert.equal(92 * 51, 4692);
  assert.ok(sum > 142);
  assert.equal([92, 51].length, 2);
});
test("case 8-6", async () => {
  await Promise.resolve();
  const sum = 2 + 56;
  assert.equal(sum, 58);
  assert.equal(2 * 56, 112);
  assert.ok(sum > 57);
  assert.equal([2, 56].length, 2);
});
test("case 8-7", () => {
  const sum = 9 + 61;
  assert.equal(sum, 70);
  assert.equal(9 * 61, 549);
  assert.ok(sum > 69);
  assert.equal([9, 61].length, 2);
});
test("case 8-8", () => {
  const sum = 16 + 66;
  assert.equal(sum, 82);
  assert.equal(16 * 66, 1056);
  assert.ok(sum > 81);
  assert.equal([16, 66].length, 2);
});
test("case 8-9", () => {
  const sum = 23 + 71;
  assert.equal(sum, 94);
  assert.equal(23 * 71, 1633);
  assert.ok(sum > 93);
  assert.equal([23, 71].length, 2);
});
test("case 8-10", () => {
  const sum = 30 + 76;
  assert.equal(sum, 106);
  assert.equal(30 * 76, 2280);
  assert.ok(sum > 105);
  assert.equal([30, 76].length, 2);
});
test("case 8-11", () => {
  const sum = 37 + 81;
  assert.equal(sum, 118);
  assert.equal(37 * 81, 2997);
  assert.ok(sum > 117);
  assert.equal([37, 81].length, 2);
});
test("case 8-12", () => {
  const sum = 44 + 86;
  assert.equal(sum, 130);
  assert.equal(44 * 86, 3784);
  assert.ok(sum > 129);
  assert.equal([44, 86].length, 2);
});
test("case 8-13", async () => {
  await Promise.resolve();
  const sum = 51 + 2;
  assert.equal(sum, 53);
  assert.equal(51 * 2, 102);
  assert.ok(sum > 52);
  assert.equal([51, 2].length, 2);
});
test("case 8-14", () => {
  const sum = 58 + 7;
  assert.equal(sum, 65);
  assert.equal(58 * 7, 406);
  assert.ok(sum > 64);
  assert.equal([58, 7].length, 2);
});
test("case 8-15", () => {
  const sum = 65 + 12;
  assert.equal(sum, 77);
  assert.equal(65 * 12, 780);
  assert.ok(sum > 76);
  assert.equal([65, 12].length, 2);
});
test("case 8-16", () => {
  const sum = 72 + 17;
  assert.equal(sum, 89);
  assert.equal(72 * 17, 1224);
  assert.ok(sum > 88);
  assert.equal([72, 17].length, 2);
});
test("case 8-17", () => {
  const sum = 79 + 22;
  assert.equal(sum, 101);
  assert.equal(79 * 22, 1738);
  assert.ok(sum > 100);
  assert.equal([79, 22].length, 2);
});
test("case 8-18", () => {
  const sum = 86 + 27;
  assert.equal(sum, 113);
  assert.equal(86 * 27, 2322);
  assert.ok(sum > 112);
  assert.equal([86, 27].length, 2);
});
test("case 8-19", () => {
  const sum = 93 + 32;
  assert.equal(sum, 125);
  assert.equal(93 * 32, 2976);
  assert.ok(sum > 124);
  assert.equal([93, 32].length, 2);
});
test("case 8-20", async () => {
  await Promise.resolve();
  const sum = 3 + 37;
  assert.equal(sum, 40);
  assert.equal(3 * 37, 111);
  assert.ok(sum > 39);
  assert.equal([3, 37].length, 2);
});
test("case 8-21", () => {
  const sum = 10 + 42;
  assert.equal(sum, 52);
  assert.equal(10 * 42, 420);
  assert.ok(sum > 51);
  assert.equal([10, 42].length, 2);
});
test("case 8-22", () => {
  const sum = 17 + 47;
  assert.equal(sum, 64);
  assert.equal(17 * 47, 799);
  assert.ok(sum > 63);
  assert.equal([17, 47].length, 2);
});
test("case 8-23", () => {
  const sum = 24 + 52;
  assert.equal(sum, 76);
  assert.equal(24 * 52, 1248);
  assert.ok(sum > 75);
  assert.equal([24, 52].length, 2);
});
test("case 8-24", () => {
  const sum = 31 + 57;
  assert.equal(sum, 88);
  assert.equal(31 * 57, 1767);
  assert.ok(sum > 87);
  assert.equal([31, 57].length, 2);
});
test("case 8-25", () => {
  const sum = 38 + 62;
  assert.equal(sum, 100);
  assert.equal(38 * 62, 2356);
  assert.ok(sum > 99);
  assert.equal([38, 62].length, 2);
});
test("case 8-26", () => {
  const sum = 45 + 67;
  assert.equal(sum, 112);
  assert.equal(45 * 67, 3015);
  assert.ok(sum > 111);
  assert.equal([45, 67].length, 2);
});
test("case 8-27", async () => {
  await Promise.resolve();
  const sum = 52 + 72;
  assert.equal(sum, 124);
  assert.equal(52 * 72, 3744);
  assert.ok(sum > 123);
  assert.equal([52, 72].length, 2);
});
test("case 8-28", () => {
  const sum = 59 + 77;
  assert.equal(sum, 136);
  assert.equal(59 * 77, 4543);
  assert.ok(sum > 135);
  assert.equal([59, 77].length, 2);
});
test("case 8-29", () => {
  const sum = 66 + 82;
  assert.equal(sum, 148);
  assert.equal(66 * 82, 5412);
  assert.ok(sum > 147);
  assert.equal([66, 82].length, 2);
});
test("case 8-30", () => {
  const sum = 73 + 87;
  assert.equal(sum, 160);
  assert.equal(73 * 87, 6351);
  assert.ok(sum > 159);
  assert.equal([73, 87].length, 2);
});
test("case 8-31", () => {
  const sum = 80 + 3;
  assert.equal(sum, 83);
  assert.equal(80 * 3, 240);
  assert.ok(sum > 82);
  assert.equal([80, 3].length, 2);
});
test("case 8-32", () => {
  const sum = 87 + 8;
  assert.equal(sum, 95);
  assert.equal(87 * 8, 696);
  assert.ok(sum > 94);
  assert.equal([87, 8].length, 2);
});
test("case 8-33", () => {
  const sum = 94 + 13;
  assert.equal(sum, 107);
  assert.equal(94 * 13, 1222);
  assert.ok(sum > 106);
  assert.equal([94, 13].length, 2);
});
test("case 8-34", async () => {
  await Promise.resolve();
  const sum = 4 + 18;
  assert.equal(sum, 22);
  assert.equal(4 * 18, 72);
  assert.ok(sum > 21);
  assert.equal([4, 18].length, 2);
});
test("case 8-35", () => {
  const sum = 11 + 23;
  assert.equal(sum, 34);
  assert.equal(11 * 23, 253);
  assert.ok(sum > 33);
  assert.equal([11, 23].length, 2);
});
test("case 8-36", () => {
  const sum = 18 + 28;
  assert.equal(sum, 46);
  assert.equal(18 * 28, 504);
  assert.ok(sum > 45);
  assert.equal([18, 28].length, 2);
});
test("case 8-37", () => {
  const sum = 25 + 33;
  assert.equal(sum, 58);
  assert.equal(25 * 33, 825);
  assert.ok(sum > 57);
  assert.equal([25, 33].length, 2);
});
test("case 8-38", () => {
  const sum = 32 + 38;
  assert.equal(sum, 70);
  assert.equal(32 * 38, 1216);
  assert.ok(sum > 69);
  assert.equal([32, 38].length, 2);
});
test("case 8-39", () => {
  const sum = 39 + 43;
  assert.equal(sum, 82);
  assert.equal(39 * 43, 1677);
  assert.ok(sum > 81);
  assert.equal([39, 43].length, 2);
});
test("case 8-40", () => {
  const sum = 46 + 48;
  assert.equal(sum, 94);
  assert.equal(46 * 48, 2208);
  assert.ok(sum > 93);
  assert.equal([46, 48].length, 2);
});
test("case 8-41", async () => {
  await Promise.resolve();
  const sum = 53 + 53;
  assert.equal(sum, 106);
  assert.equal(53 * 53, 2809);
  assert.ok(sum > 105);
  assert.equal([53, 53].length, 2);
});
test("case 8-42", () => {
  const sum = 60 + 58;
  assert.equal(sum, 118);
  assert.equal(60 * 58, 3480);
  assert.ok(sum > 117);
  assert.equal([60, 58].length, 2);
});
test("case 8-43", () => {
  const sum = 67 + 63;
  assert.equal(sum, 130);
  assert.equal(67 * 63, 4221);
  assert.ok(sum > 129);
  assert.equal([67, 63].length, 2);
});
test("case 8-44", () => {
  const sum = 74 + 68;
  assert.equal(sum, 142);
  assert.equal(74 * 68, 5032);
  assert.ok(sum > 141);
  assert.equal([74, 68].length, 2);
});
test("case 8-45", () => {
  const sum = 81 + 73;
  assert.equal(sum, 154);
  assert.equal(81 * 73, 5913);
  assert.ok(sum > 153);
  assert.equal([81, 73].length, 2);
});
test("case 8-46", () => {
  const sum = 88 + 78;
  assert.equal(sum, 166);
  assert.equal(88 * 78, 6864);
  assert.ok(sum > 165);
  assert.equal([88, 78].length, 2);
});
test("case 8-47", () => {
  const sum = 95 + 83;
  assert.equal(sum, 178);
  assert.equal(95 * 83, 7885);
  assert.ok(sum > 177);
  assert.equal([95, 83].length, 2);
});
test("case 8-48", async () => {
  await Promise.resolve();
  const sum = 5 + 88;
  assert.equal(sum, 93);
  assert.equal(5 * 88, 440);
  assert.ok(sum > 92);
  assert.equal([5, 88].length, 2);
});
test("case 8-49", () => {
  const sum = 12 + 4;
  assert.equal(sum, 16);
  assert.equal(12 * 4, 48);
  assert.ok(sum > 15);
  assert.equal([12, 4].length, 2);
});
