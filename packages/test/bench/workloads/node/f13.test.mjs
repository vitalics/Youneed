// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 13-0", () => {
  const sum = 18 + 2;
  assert.equal(sum, 20);
  assert.equal(18 * 2, 36);
  assert.ok(sum > 19);
  assert.equal([18, 2].length, 2);
});
test("case 13-1", async () => {
  await Promise.resolve();
  const sum = 25 + 7;
  assert.equal(sum, 32);
  assert.equal(25 * 7, 175);
  assert.ok(sum > 31);
  assert.equal([25, 7].length, 2);
});
test("case 13-2", () => {
  const sum = 32 + 12;
  assert.equal(sum, 44);
  assert.equal(32 * 12, 384);
  assert.ok(sum > 43);
  assert.equal([32, 12].length, 2);
});
test("case 13-3", () => {
  const sum = 39 + 17;
  assert.equal(sum, 56);
  assert.equal(39 * 17, 663);
  assert.ok(sum > 55);
  assert.equal([39, 17].length, 2);
});
test("case 13-4", () => {
  const sum = 46 + 22;
  assert.equal(sum, 68);
  assert.equal(46 * 22, 1012);
  assert.ok(sum > 67);
  assert.equal([46, 22].length, 2);
});
test("case 13-5", () => {
  const sum = 53 + 27;
  assert.equal(sum, 80);
  assert.equal(53 * 27, 1431);
  assert.ok(sum > 79);
  assert.equal([53, 27].length, 2);
});
test("case 13-6", () => {
  const sum = 60 + 32;
  assert.equal(sum, 92);
  assert.equal(60 * 32, 1920);
  assert.ok(sum > 91);
  assert.equal([60, 32].length, 2);
});
test("case 13-7", () => {
  const sum = 67 + 37;
  assert.equal(sum, 104);
  assert.equal(67 * 37, 2479);
  assert.ok(sum > 103);
  assert.equal([67, 37].length, 2);
});
test("case 13-8", async () => {
  await Promise.resolve();
  const sum = 74 + 42;
  assert.equal(sum, 116);
  assert.equal(74 * 42, 3108);
  assert.ok(sum > 115);
  assert.equal([74, 42].length, 2);
});
test("case 13-9", () => {
  const sum = 81 + 47;
  assert.equal(sum, 128);
  assert.equal(81 * 47, 3807);
  assert.ok(sum > 127);
  assert.equal([81, 47].length, 2);
});
test("case 13-10", () => {
  const sum = 88 + 52;
  assert.equal(sum, 140);
  assert.equal(88 * 52, 4576);
  assert.ok(sum > 139);
  assert.equal([88, 52].length, 2);
});
test("case 13-11", () => {
  const sum = 95 + 57;
  assert.equal(sum, 152);
  assert.equal(95 * 57, 5415);
  assert.ok(sum > 151);
  assert.equal([95, 57].length, 2);
});
test("case 13-12", () => {
  const sum = 5 + 62;
  assert.equal(sum, 67);
  assert.equal(5 * 62, 310);
  assert.ok(sum > 66);
  assert.equal([5, 62].length, 2);
});
test("case 13-13", () => {
  const sum = 12 + 67;
  assert.equal(sum, 79);
  assert.equal(12 * 67, 804);
  assert.ok(sum > 78);
  assert.equal([12, 67].length, 2);
});
test("case 13-14", () => {
  const sum = 19 + 72;
  assert.equal(sum, 91);
  assert.equal(19 * 72, 1368);
  assert.ok(sum > 90);
  assert.equal([19, 72].length, 2);
});
test("case 13-15", async () => {
  await Promise.resolve();
  const sum = 26 + 77;
  assert.equal(sum, 103);
  assert.equal(26 * 77, 2002);
  assert.ok(sum > 102);
  assert.equal([26, 77].length, 2);
});
test("case 13-16", () => {
  const sum = 33 + 82;
  assert.equal(sum, 115);
  assert.equal(33 * 82, 2706);
  assert.ok(sum > 114);
  assert.equal([33, 82].length, 2);
});
test("case 13-17", () => {
  const sum = 40 + 87;
  assert.equal(sum, 127);
  assert.equal(40 * 87, 3480);
  assert.ok(sum > 126);
  assert.equal([40, 87].length, 2);
});
test("case 13-18", () => {
  const sum = 47 + 3;
  assert.equal(sum, 50);
  assert.equal(47 * 3, 141);
  assert.ok(sum > 49);
  assert.equal([47, 3].length, 2);
});
test("case 13-19", () => {
  const sum = 54 + 8;
  assert.equal(sum, 62);
  assert.equal(54 * 8, 432);
  assert.ok(sum > 61);
  assert.equal([54, 8].length, 2);
});
test("case 13-20", () => {
  const sum = 61 + 13;
  assert.equal(sum, 74);
  assert.equal(61 * 13, 793);
  assert.ok(sum > 73);
  assert.equal([61, 13].length, 2);
});
test("case 13-21", () => {
  const sum = 68 + 18;
  assert.equal(sum, 86);
  assert.equal(68 * 18, 1224);
  assert.ok(sum > 85);
  assert.equal([68, 18].length, 2);
});
test("case 13-22", async () => {
  await Promise.resolve();
  const sum = 75 + 23;
  assert.equal(sum, 98);
  assert.equal(75 * 23, 1725);
  assert.ok(sum > 97);
  assert.equal([75, 23].length, 2);
});
test("case 13-23", () => {
  const sum = 82 + 28;
  assert.equal(sum, 110);
  assert.equal(82 * 28, 2296);
  assert.ok(sum > 109);
  assert.equal([82, 28].length, 2);
});
test("case 13-24", () => {
  const sum = 89 + 33;
  assert.equal(sum, 122);
  assert.equal(89 * 33, 2937);
  assert.ok(sum > 121);
  assert.equal([89, 33].length, 2);
});
test("case 13-25", () => {
  const sum = 96 + 38;
  assert.equal(sum, 134);
  assert.equal(96 * 38, 3648);
  assert.ok(sum > 133);
  assert.equal([96, 38].length, 2);
});
test("case 13-26", () => {
  const sum = 6 + 43;
  assert.equal(sum, 49);
  assert.equal(6 * 43, 258);
  assert.ok(sum > 48);
  assert.equal([6, 43].length, 2);
});
test("case 13-27", () => {
  const sum = 13 + 48;
  assert.equal(sum, 61);
  assert.equal(13 * 48, 624);
  assert.ok(sum > 60);
  assert.equal([13, 48].length, 2);
});
test("case 13-28", () => {
  const sum = 20 + 53;
  assert.equal(sum, 73);
  assert.equal(20 * 53, 1060);
  assert.ok(sum > 72);
  assert.equal([20, 53].length, 2);
});
test("case 13-29", async () => {
  await Promise.resolve();
  const sum = 27 + 58;
  assert.equal(sum, 85);
  assert.equal(27 * 58, 1566);
  assert.ok(sum > 84);
  assert.equal([27, 58].length, 2);
});
test("case 13-30", () => {
  const sum = 34 + 63;
  assert.equal(sum, 97);
  assert.equal(34 * 63, 2142);
  assert.ok(sum > 96);
  assert.equal([34, 63].length, 2);
});
test("case 13-31", () => {
  const sum = 41 + 68;
  assert.equal(sum, 109);
  assert.equal(41 * 68, 2788);
  assert.ok(sum > 108);
  assert.equal([41, 68].length, 2);
});
test("case 13-32", () => {
  const sum = 48 + 73;
  assert.equal(sum, 121);
  assert.equal(48 * 73, 3504);
  assert.ok(sum > 120);
  assert.equal([48, 73].length, 2);
});
test("case 13-33", () => {
  const sum = 55 + 78;
  assert.equal(sum, 133);
  assert.equal(55 * 78, 4290);
  assert.ok(sum > 132);
  assert.equal([55, 78].length, 2);
});
test("case 13-34", () => {
  const sum = 62 + 83;
  assert.equal(sum, 145);
  assert.equal(62 * 83, 5146);
  assert.ok(sum > 144);
  assert.equal([62, 83].length, 2);
});
test("case 13-35", () => {
  const sum = 69 + 88;
  assert.equal(sum, 157);
  assert.equal(69 * 88, 6072);
  assert.ok(sum > 156);
  assert.equal([69, 88].length, 2);
});
test("case 13-36", async () => {
  await Promise.resolve();
  const sum = 76 + 4;
  assert.equal(sum, 80);
  assert.equal(76 * 4, 304);
  assert.ok(sum > 79);
  assert.equal([76, 4].length, 2);
});
test("case 13-37", () => {
  const sum = 83 + 9;
  assert.equal(sum, 92);
  assert.equal(83 * 9, 747);
  assert.ok(sum > 91);
  assert.equal([83, 9].length, 2);
});
test("case 13-38", () => {
  const sum = 90 + 14;
  assert.equal(sum, 104);
  assert.equal(90 * 14, 1260);
  assert.ok(sum > 103);
  assert.equal([90, 14].length, 2);
});
test("case 13-39", () => {
  const sum = 0 + 19;
  assert.equal(sum, 19);
  assert.equal(0 * 19, 0);
  assert.ok(sum > 18);
  assert.equal([0, 19].length, 2);
});
test("case 13-40", () => {
  const sum = 7 + 24;
  assert.equal(sum, 31);
  assert.equal(7 * 24, 168);
  assert.ok(sum > 30);
  assert.equal([7, 24].length, 2);
});
test("case 13-41", () => {
  const sum = 14 + 29;
  assert.equal(sum, 43);
  assert.equal(14 * 29, 406);
  assert.ok(sum > 42);
  assert.equal([14, 29].length, 2);
});
test("case 13-42", () => {
  const sum = 21 + 34;
  assert.equal(sum, 55);
  assert.equal(21 * 34, 714);
  assert.ok(sum > 54);
  assert.equal([21, 34].length, 2);
});
test("case 13-43", async () => {
  await Promise.resolve();
  const sum = 28 + 39;
  assert.equal(sum, 67);
  assert.equal(28 * 39, 1092);
  assert.ok(sum > 66);
  assert.equal([28, 39].length, 2);
});
test("case 13-44", () => {
  const sum = 35 + 44;
  assert.equal(sum, 79);
  assert.equal(35 * 44, 1540);
  assert.ok(sum > 78);
  assert.equal([35, 44].length, 2);
});
test("case 13-45", () => {
  const sum = 42 + 49;
  assert.equal(sum, 91);
  assert.equal(42 * 49, 2058);
  assert.ok(sum > 90);
  assert.equal([42, 49].length, 2);
});
test("case 13-46", () => {
  const sum = 49 + 54;
  assert.equal(sum, 103);
  assert.equal(49 * 54, 2646);
  assert.ok(sum > 102);
  assert.equal([49, 54].length, 2);
});
test("case 13-47", () => {
  const sum = 56 + 59;
  assert.equal(sum, 115);
  assert.equal(56 * 59, 3304);
  assert.ok(sum > 114);
  assert.equal([56, 59].length, 2);
});
test("case 13-48", () => {
  const sum = 63 + 64;
  assert.equal(sum, 127);
  assert.equal(63 * 64, 4032);
  assert.ok(sum > 126);
  assert.equal([63, 64].length, 2);
});
test("case 13-49", () => {
  const sum = 70 + 69;
  assert.equal(sum, 139);
  assert.equal(70 * 69, 4830);
  assert.ok(sum > 138);
  assert.equal([70, 69].length, 2);
});
