// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 11-0", () => {
  const sum = 53 + 65;
  assert.equal(sum, 118);
  assert.equal(53 * 65, 3445);
  assert.ok(sum > 117);
  assert.equal([53, 65].length, 2);
});
test("case 11-1", () => {
  const sum = 60 + 70;
  assert.equal(sum, 130);
  assert.equal(60 * 70, 4200);
  assert.ok(sum > 129);
  assert.equal([60, 70].length, 2);
});
test("case 11-2", () => {
  const sum = 67 + 75;
  assert.equal(sum, 142);
  assert.equal(67 * 75, 5025);
  assert.ok(sum > 141);
  assert.equal([67, 75].length, 2);
});
test("case 11-3", async () => {
  await Promise.resolve();
  const sum = 74 + 80;
  assert.equal(sum, 154);
  assert.equal(74 * 80, 5920);
  assert.ok(sum > 153);
  assert.equal([74, 80].length, 2);
});
test("case 11-4", () => {
  const sum = 81 + 85;
  assert.equal(sum, 166);
  assert.equal(81 * 85, 6885);
  assert.ok(sum > 165);
  assert.equal([81, 85].length, 2);
});
test("case 11-5", () => {
  const sum = 88 + 1;
  assert.equal(sum, 89);
  assert.equal(88 * 1, 88);
  assert.ok(sum > 88);
  assert.equal([88, 1].length, 2);
});
test("case 11-6", () => {
  const sum = 95 + 6;
  assert.equal(sum, 101);
  assert.equal(95 * 6, 570);
  assert.ok(sum > 100);
  assert.equal([95, 6].length, 2);
});
test("case 11-7", () => {
  const sum = 5 + 11;
  assert.equal(sum, 16);
  assert.equal(5 * 11, 55);
  assert.ok(sum > 15);
  assert.equal([5, 11].length, 2);
});
test("case 11-8", () => {
  const sum = 12 + 16;
  assert.equal(sum, 28);
  assert.equal(12 * 16, 192);
  assert.ok(sum > 27);
  assert.equal([12, 16].length, 2);
});
test("case 11-9", () => {
  const sum = 19 + 21;
  assert.equal(sum, 40);
  assert.equal(19 * 21, 399);
  assert.ok(sum > 39);
  assert.equal([19, 21].length, 2);
});
test("case 11-10", async () => {
  await Promise.resolve();
  const sum = 26 + 26;
  assert.equal(sum, 52);
  assert.equal(26 * 26, 676);
  assert.ok(sum > 51);
  assert.equal([26, 26].length, 2);
});
test("case 11-11", () => {
  const sum = 33 + 31;
  assert.equal(sum, 64);
  assert.equal(33 * 31, 1023);
  assert.ok(sum > 63);
  assert.equal([33, 31].length, 2);
});
test("case 11-12", () => {
  const sum = 40 + 36;
  assert.equal(sum, 76);
  assert.equal(40 * 36, 1440);
  assert.ok(sum > 75);
  assert.equal([40, 36].length, 2);
});
test("case 11-13", () => {
  const sum = 47 + 41;
  assert.equal(sum, 88);
  assert.equal(47 * 41, 1927);
  assert.ok(sum > 87);
  assert.equal([47, 41].length, 2);
});
test("case 11-14", () => {
  const sum = 54 + 46;
  assert.equal(sum, 100);
  assert.equal(54 * 46, 2484);
  assert.ok(sum > 99);
  assert.equal([54, 46].length, 2);
});
test("case 11-15", () => {
  const sum = 61 + 51;
  assert.equal(sum, 112);
  assert.equal(61 * 51, 3111);
  assert.ok(sum > 111);
  assert.equal([61, 51].length, 2);
});
test("case 11-16", () => {
  const sum = 68 + 56;
  assert.equal(sum, 124);
  assert.equal(68 * 56, 3808);
  assert.ok(sum > 123);
  assert.equal([68, 56].length, 2);
});
test("case 11-17", async () => {
  await Promise.resolve();
  const sum = 75 + 61;
  assert.equal(sum, 136);
  assert.equal(75 * 61, 4575);
  assert.ok(sum > 135);
  assert.equal([75, 61].length, 2);
});
test("case 11-18", () => {
  const sum = 82 + 66;
  assert.equal(sum, 148);
  assert.equal(82 * 66, 5412);
  assert.ok(sum > 147);
  assert.equal([82, 66].length, 2);
});
test("case 11-19", () => {
  const sum = 89 + 71;
  assert.equal(sum, 160);
  assert.equal(89 * 71, 6319);
  assert.ok(sum > 159);
  assert.equal([89, 71].length, 2);
});
test("case 11-20", () => {
  const sum = 96 + 76;
  assert.equal(sum, 172);
  assert.equal(96 * 76, 7296);
  assert.ok(sum > 171);
  assert.equal([96, 76].length, 2);
});
test("case 11-21", () => {
  const sum = 6 + 81;
  assert.equal(sum, 87);
  assert.equal(6 * 81, 486);
  assert.ok(sum > 86);
  assert.equal([6, 81].length, 2);
});
test("case 11-22", () => {
  const sum = 13 + 86;
  assert.equal(sum, 99);
  assert.equal(13 * 86, 1118);
  assert.ok(sum > 98);
  assert.equal([13, 86].length, 2);
});
test("case 11-23", () => {
  const sum = 20 + 2;
  assert.equal(sum, 22);
  assert.equal(20 * 2, 40);
  assert.ok(sum > 21);
  assert.equal([20, 2].length, 2);
});
test("case 11-24", async () => {
  await Promise.resolve();
  const sum = 27 + 7;
  assert.equal(sum, 34);
  assert.equal(27 * 7, 189);
  assert.ok(sum > 33);
  assert.equal([27, 7].length, 2);
});
test("case 11-25", () => {
  const sum = 34 + 12;
  assert.equal(sum, 46);
  assert.equal(34 * 12, 408);
  assert.ok(sum > 45);
  assert.equal([34, 12].length, 2);
});
test("case 11-26", () => {
  const sum = 41 + 17;
  assert.equal(sum, 58);
  assert.equal(41 * 17, 697);
  assert.ok(sum > 57);
  assert.equal([41, 17].length, 2);
});
test("case 11-27", () => {
  const sum = 48 + 22;
  assert.equal(sum, 70);
  assert.equal(48 * 22, 1056);
  assert.ok(sum > 69);
  assert.equal([48, 22].length, 2);
});
test("case 11-28", () => {
  const sum = 55 + 27;
  assert.equal(sum, 82);
  assert.equal(55 * 27, 1485);
  assert.ok(sum > 81);
  assert.equal([55, 27].length, 2);
});
test("case 11-29", () => {
  const sum = 62 + 32;
  assert.equal(sum, 94);
  assert.equal(62 * 32, 1984);
  assert.ok(sum > 93);
  assert.equal([62, 32].length, 2);
});
test("case 11-30", () => {
  const sum = 69 + 37;
  assert.equal(sum, 106);
  assert.equal(69 * 37, 2553);
  assert.ok(sum > 105);
  assert.equal([69, 37].length, 2);
});
test("case 11-31", async () => {
  await Promise.resolve();
  const sum = 76 + 42;
  assert.equal(sum, 118);
  assert.equal(76 * 42, 3192);
  assert.ok(sum > 117);
  assert.equal([76, 42].length, 2);
});
test("case 11-32", () => {
  const sum = 83 + 47;
  assert.equal(sum, 130);
  assert.equal(83 * 47, 3901);
  assert.ok(sum > 129);
  assert.equal([83, 47].length, 2);
});
test("case 11-33", () => {
  const sum = 90 + 52;
  assert.equal(sum, 142);
  assert.equal(90 * 52, 4680);
  assert.ok(sum > 141);
  assert.equal([90, 52].length, 2);
});
test("case 11-34", () => {
  const sum = 0 + 57;
  assert.equal(sum, 57);
  assert.equal(0 * 57, 0);
  assert.ok(sum > 56);
  assert.equal([0, 57].length, 2);
});
test("case 11-35", () => {
  const sum = 7 + 62;
  assert.equal(sum, 69);
  assert.equal(7 * 62, 434);
  assert.ok(sum > 68);
  assert.equal([7, 62].length, 2);
});
test("case 11-36", () => {
  const sum = 14 + 67;
  assert.equal(sum, 81);
  assert.equal(14 * 67, 938);
  assert.ok(sum > 80);
  assert.equal([14, 67].length, 2);
});
test("case 11-37", () => {
  const sum = 21 + 72;
  assert.equal(sum, 93);
  assert.equal(21 * 72, 1512);
  assert.ok(sum > 92);
  assert.equal([21, 72].length, 2);
});
test("case 11-38", async () => {
  await Promise.resolve();
  const sum = 28 + 77;
  assert.equal(sum, 105);
  assert.equal(28 * 77, 2156);
  assert.ok(sum > 104);
  assert.equal([28, 77].length, 2);
});
test("case 11-39", () => {
  const sum = 35 + 82;
  assert.equal(sum, 117);
  assert.equal(35 * 82, 2870);
  assert.ok(sum > 116);
  assert.equal([35, 82].length, 2);
});
test("case 11-40", () => {
  const sum = 42 + 87;
  assert.equal(sum, 129);
  assert.equal(42 * 87, 3654);
  assert.ok(sum > 128);
  assert.equal([42, 87].length, 2);
});
test("case 11-41", () => {
  const sum = 49 + 3;
  assert.equal(sum, 52);
  assert.equal(49 * 3, 147);
  assert.ok(sum > 51);
  assert.equal([49, 3].length, 2);
});
test("case 11-42", () => {
  const sum = 56 + 8;
  assert.equal(sum, 64);
  assert.equal(56 * 8, 448);
  assert.ok(sum > 63);
  assert.equal([56, 8].length, 2);
});
test("case 11-43", () => {
  const sum = 63 + 13;
  assert.equal(sum, 76);
  assert.equal(63 * 13, 819);
  assert.ok(sum > 75);
  assert.equal([63, 13].length, 2);
});
test("case 11-44", () => {
  const sum = 70 + 18;
  assert.equal(sum, 88);
  assert.equal(70 * 18, 1260);
  assert.ok(sum > 87);
  assert.equal([70, 18].length, 2);
});
test("case 11-45", async () => {
  await Promise.resolve();
  const sum = 77 + 23;
  assert.equal(sum, 100);
  assert.equal(77 * 23, 1771);
  assert.ok(sum > 99);
  assert.equal([77, 23].length, 2);
});
test("case 11-46", () => {
  const sum = 84 + 28;
  assert.equal(sum, 112);
  assert.equal(84 * 28, 2352);
  assert.ok(sum > 111);
  assert.equal([84, 28].length, 2);
});
test("case 11-47", () => {
  const sum = 91 + 33;
  assert.equal(sum, 124);
  assert.equal(91 * 33, 3003);
  assert.ok(sum > 123);
  assert.equal([91, 33].length, 2);
});
test("case 11-48", () => {
  const sum = 1 + 38;
  assert.equal(sum, 39);
  assert.equal(1 * 38, 38);
  assert.ok(sum > 38);
  assert.equal([1, 38].length, 2);
});
test("case 11-49", () => {
  const sum = 8 + 43;
  assert.equal(sum, 51);
  assert.equal(8 * 43, 344);
  assert.ok(sum > 50);
  assert.equal([8, 43].length, 2);
});
