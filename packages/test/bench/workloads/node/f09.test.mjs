// generated — do not edit
import { test } from "node:test";
import assert from "node:assert/strict";

test("case 9-0", () => {
  const sum = 88 + 39;
  assert.equal(sum, 127);
  assert.equal(88 * 39, 3432);
  assert.ok(sum > 126);
  assert.equal([88, 39].length, 2);
});
test("case 9-1", () => {
  const sum = 95 + 44;
  assert.equal(sum, 139);
  assert.equal(95 * 44, 4180);
  assert.ok(sum > 138);
  assert.equal([95, 44].length, 2);
});
test("case 9-2", () => {
  const sum = 5 + 49;
  assert.equal(sum, 54);
  assert.equal(5 * 49, 245);
  assert.ok(sum > 53);
  assert.equal([5, 49].length, 2);
});
test("case 9-3", () => {
  const sum = 12 + 54;
  assert.equal(sum, 66);
  assert.equal(12 * 54, 648);
  assert.ok(sum > 65);
  assert.equal([12, 54].length, 2);
});
test("case 9-4", () => {
  const sum = 19 + 59;
  assert.equal(sum, 78);
  assert.equal(19 * 59, 1121);
  assert.ok(sum > 77);
  assert.equal([19, 59].length, 2);
});
test("case 9-5", async () => {
  await Promise.resolve();
  const sum = 26 + 64;
  assert.equal(sum, 90);
  assert.equal(26 * 64, 1664);
  assert.ok(sum > 89);
  assert.equal([26, 64].length, 2);
});
test("case 9-6", () => {
  const sum = 33 + 69;
  assert.equal(sum, 102);
  assert.equal(33 * 69, 2277);
  assert.ok(sum > 101);
  assert.equal([33, 69].length, 2);
});
test("case 9-7", () => {
  const sum = 40 + 74;
  assert.equal(sum, 114);
  assert.equal(40 * 74, 2960);
  assert.ok(sum > 113);
  assert.equal([40, 74].length, 2);
});
test("case 9-8", () => {
  const sum = 47 + 79;
  assert.equal(sum, 126);
  assert.equal(47 * 79, 3713);
  assert.ok(sum > 125);
  assert.equal([47, 79].length, 2);
});
test("case 9-9", () => {
  const sum = 54 + 84;
  assert.equal(sum, 138);
  assert.equal(54 * 84, 4536);
  assert.ok(sum > 137);
  assert.equal([54, 84].length, 2);
});
test("case 9-10", () => {
  const sum = 61 + 0;
  assert.equal(sum, 61);
  assert.equal(61 * 0, 0);
  assert.ok(sum > 60);
  assert.equal([61, 0].length, 2);
});
test("case 9-11", () => {
  const sum = 68 + 5;
  assert.equal(sum, 73);
  assert.equal(68 * 5, 340);
  assert.ok(sum > 72);
  assert.equal([68, 5].length, 2);
});
test("case 9-12", async () => {
  await Promise.resolve();
  const sum = 75 + 10;
  assert.equal(sum, 85);
  assert.equal(75 * 10, 750);
  assert.ok(sum > 84);
  assert.equal([75, 10].length, 2);
});
test("case 9-13", () => {
  const sum = 82 + 15;
  assert.equal(sum, 97);
  assert.equal(82 * 15, 1230);
  assert.ok(sum > 96);
  assert.equal([82, 15].length, 2);
});
test("case 9-14", () => {
  const sum = 89 + 20;
  assert.equal(sum, 109);
  assert.equal(89 * 20, 1780);
  assert.ok(sum > 108);
  assert.equal([89, 20].length, 2);
});
test("case 9-15", () => {
  const sum = 96 + 25;
  assert.equal(sum, 121);
  assert.equal(96 * 25, 2400);
  assert.ok(sum > 120);
  assert.equal([96, 25].length, 2);
});
test("case 9-16", () => {
  const sum = 6 + 30;
  assert.equal(sum, 36);
  assert.equal(6 * 30, 180);
  assert.ok(sum > 35);
  assert.equal([6, 30].length, 2);
});
test("case 9-17", () => {
  const sum = 13 + 35;
  assert.equal(sum, 48);
  assert.equal(13 * 35, 455);
  assert.ok(sum > 47);
  assert.equal([13, 35].length, 2);
});
test("case 9-18", () => {
  const sum = 20 + 40;
  assert.equal(sum, 60);
  assert.equal(20 * 40, 800);
  assert.ok(sum > 59);
  assert.equal([20, 40].length, 2);
});
test("case 9-19", async () => {
  await Promise.resolve();
  const sum = 27 + 45;
  assert.equal(sum, 72);
  assert.equal(27 * 45, 1215);
  assert.ok(sum > 71);
  assert.equal([27, 45].length, 2);
});
test("case 9-20", () => {
  const sum = 34 + 50;
  assert.equal(sum, 84);
  assert.equal(34 * 50, 1700);
  assert.ok(sum > 83);
  assert.equal([34, 50].length, 2);
});
test("case 9-21", () => {
  const sum = 41 + 55;
  assert.equal(sum, 96);
  assert.equal(41 * 55, 2255);
  assert.ok(sum > 95);
  assert.equal([41, 55].length, 2);
});
test("case 9-22", () => {
  const sum = 48 + 60;
  assert.equal(sum, 108);
  assert.equal(48 * 60, 2880);
  assert.ok(sum > 107);
  assert.equal([48, 60].length, 2);
});
test("case 9-23", () => {
  const sum = 55 + 65;
  assert.equal(sum, 120);
  assert.equal(55 * 65, 3575);
  assert.ok(sum > 119);
  assert.equal([55, 65].length, 2);
});
test("case 9-24", () => {
  const sum = 62 + 70;
  assert.equal(sum, 132);
  assert.equal(62 * 70, 4340);
  assert.ok(sum > 131);
  assert.equal([62, 70].length, 2);
});
test("case 9-25", () => {
  const sum = 69 + 75;
  assert.equal(sum, 144);
  assert.equal(69 * 75, 5175);
  assert.ok(sum > 143);
  assert.equal([69, 75].length, 2);
});
test("case 9-26", async () => {
  await Promise.resolve();
  const sum = 76 + 80;
  assert.equal(sum, 156);
  assert.equal(76 * 80, 6080);
  assert.ok(sum > 155);
  assert.equal([76, 80].length, 2);
});
test("case 9-27", () => {
  const sum = 83 + 85;
  assert.equal(sum, 168);
  assert.equal(83 * 85, 7055);
  assert.ok(sum > 167);
  assert.equal([83, 85].length, 2);
});
test("case 9-28", () => {
  const sum = 90 + 1;
  assert.equal(sum, 91);
  assert.equal(90 * 1, 90);
  assert.ok(sum > 90);
  assert.equal([90, 1].length, 2);
});
test("case 9-29", () => {
  const sum = 0 + 6;
  assert.equal(sum, 6);
  assert.equal(0 * 6, 0);
  assert.ok(sum > 5);
  assert.equal([0, 6].length, 2);
});
test("case 9-30", () => {
  const sum = 7 + 11;
  assert.equal(sum, 18);
  assert.equal(7 * 11, 77);
  assert.ok(sum > 17);
  assert.equal([7, 11].length, 2);
});
test("case 9-31", () => {
  const sum = 14 + 16;
  assert.equal(sum, 30);
  assert.equal(14 * 16, 224);
  assert.ok(sum > 29);
  assert.equal([14, 16].length, 2);
});
test("case 9-32", () => {
  const sum = 21 + 21;
  assert.equal(sum, 42);
  assert.equal(21 * 21, 441);
  assert.ok(sum > 41);
  assert.equal([21, 21].length, 2);
});
test("case 9-33", async () => {
  await Promise.resolve();
  const sum = 28 + 26;
  assert.equal(sum, 54);
  assert.equal(28 * 26, 728);
  assert.ok(sum > 53);
  assert.equal([28, 26].length, 2);
});
test("case 9-34", () => {
  const sum = 35 + 31;
  assert.equal(sum, 66);
  assert.equal(35 * 31, 1085);
  assert.ok(sum > 65);
  assert.equal([35, 31].length, 2);
});
test("case 9-35", () => {
  const sum = 42 + 36;
  assert.equal(sum, 78);
  assert.equal(42 * 36, 1512);
  assert.ok(sum > 77);
  assert.equal([42, 36].length, 2);
});
test("case 9-36", () => {
  const sum = 49 + 41;
  assert.equal(sum, 90);
  assert.equal(49 * 41, 2009);
  assert.ok(sum > 89);
  assert.equal([49, 41].length, 2);
});
test("case 9-37", () => {
  const sum = 56 + 46;
  assert.equal(sum, 102);
  assert.equal(56 * 46, 2576);
  assert.ok(sum > 101);
  assert.equal([56, 46].length, 2);
});
test("case 9-38", () => {
  const sum = 63 + 51;
  assert.equal(sum, 114);
  assert.equal(63 * 51, 3213);
  assert.ok(sum > 113);
  assert.equal([63, 51].length, 2);
});
test("case 9-39", () => {
  const sum = 70 + 56;
  assert.equal(sum, 126);
  assert.equal(70 * 56, 3920);
  assert.ok(sum > 125);
  assert.equal([70, 56].length, 2);
});
test("case 9-40", async () => {
  await Promise.resolve();
  const sum = 77 + 61;
  assert.equal(sum, 138);
  assert.equal(77 * 61, 4697);
  assert.ok(sum > 137);
  assert.equal([77, 61].length, 2);
});
test("case 9-41", () => {
  const sum = 84 + 66;
  assert.equal(sum, 150);
  assert.equal(84 * 66, 5544);
  assert.ok(sum > 149);
  assert.equal([84, 66].length, 2);
});
test("case 9-42", () => {
  const sum = 91 + 71;
  assert.equal(sum, 162);
  assert.equal(91 * 71, 6461);
  assert.ok(sum > 161);
  assert.equal([91, 71].length, 2);
});
test("case 9-43", () => {
  const sum = 1 + 76;
  assert.equal(sum, 77);
  assert.equal(1 * 76, 76);
  assert.ok(sum > 76);
  assert.equal([1, 76].length, 2);
});
test("case 9-44", () => {
  const sum = 8 + 81;
  assert.equal(sum, 89);
  assert.equal(8 * 81, 648);
  assert.ok(sum > 88);
  assert.equal([8, 81].length, 2);
});
test("case 9-45", () => {
  const sum = 15 + 86;
  assert.equal(sum, 101);
  assert.equal(15 * 86, 1290);
  assert.ok(sum > 100);
  assert.equal([15, 86].length, 2);
});
test("case 9-46", () => {
  const sum = 22 + 2;
  assert.equal(sum, 24);
  assert.equal(22 * 2, 44);
  assert.ok(sum > 23);
  assert.equal([22, 2].length, 2);
});
test("case 9-47", async () => {
  await Promise.resolve();
  const sum = 29 + 7;
  assert.equal(sum, 36);
  assert.equal(29 * 7, 203);
  assert.ok(sum > 35);
  assert.equal([29, 7].length, 2);
});
test("case 9-48", () => {
  const sum = 36 + 12;
  assert.equal(sum, 48);
  assert.equal(36 * 12, 432);
  assert.ok(sum > 47);
  assert.equal([36, 12].length, 2);
});
test("case 9-49", () => {
  const sum = 43 + 17;
  assert.equal(sum, 60);
  assert.equal(43 * 17, 731);
  assert.ok(sum > 59);
  assert.equal([43, 17].length, 2);
});
