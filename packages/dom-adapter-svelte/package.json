{
  "name": "@youneed/dom-adapter-svelte",
  "version": "0.1.0",
  "description": "Use @youneed/dom components inside Svelte (a use: action) and Svelte components inside @youneed/dom — type-safe and refactor-friendly (pass the component, not a tag string).",
  "type": "module",
  "license": "ISC",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsx tests/to-svelte.test.ts && node --conditions browser --import tsx tests/from-svelte.test.ts"
  },
  "peerDependencies": {
    "svelte": ">=5"
  },
  "devDependencies": {
    "@youneed/dom": "workspace:*",
    "svelte": "^5.0.0"
  }
}
