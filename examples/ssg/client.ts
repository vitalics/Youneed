// Client entry — importing the app registers <counter-app>, which makes the
// browser upgrade the server-rendered element and hydrate it (the component
// reuses the Declarative-Shadow-DOM root, then binds events on re-render).
import "./app.ts";
